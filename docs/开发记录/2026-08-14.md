# 2026-08-14 开发记录

> 提交：`4d2e96e`（v0.8.1 发布）+ 未提交的「移除兜底模型」改动

## 消息气泡渲染优化 & 发布 v0.8.1

- 代码块标题栏空隙修复：根因是 Tailwind Typography 插件的 `.prose :where(pre)` 设置了 `margin-top/bottom: 1.14em`（unlayered CSS 压过工具类），改 `not-prose` 隔离
- 折叠块（思考/工具/代码）内外边距统一：容器 `mt-1.5 mb-1`、标题栏 `px-3 py-1.5`、内容区 `px-3 py-2`
- diff 结果默认展开且可手动收起：`useState(isDiffResult)` 初始展开 + `useEffect` 流式后到时补展开；多工具同组含 diff 自动展开（ToolGroupView）
- markdown 一级标题字号 30px→21px（`[&_h1]:[font-size:1.5em]`，贴合聊天语境）
- 字号语义化：5 档变量 `--text-body/detail/caption/code/meta`（14/13/12/11/10px），替换散落的 `text-[1xpx]`；body/detail 由设置滑杆驱动，caption/code/meta 固定档
- 代码块语言识别：正则 `[\w+#-]*` 支持 c++/c#，LANG_LABELS 映射表显示标准名

## 上下文文件规范化为 AGENTS.md

- 模板 `template/CLAUDE.md` → `template/AGENTS.md`（Pi 按 `AGENTS.override.md > AGENTS.md > CLAUDE.md` 优先级只注入一个；AGENTS.md 兼容 Pi/Codex 通用 agent）
- prompts.ts 6 处硬编码 CLAUDE.md → AGENTS.md；新增「上下文文件补充读取」规则（主上下文为 AGENTS.md 时顺带补读项目根 CLAUDE.md）
- 文档记录规范统一命名「开发记录.md」（快照 + 按日期分文件），dev-docs skill / prompts.ts / 模板三处对齐

## 注释规范补进 CLAUDE.md

- 2.1 节新增「注释规范」子节：只写 WHY 不写 WHAT、禁止行号/硬路径线索、重要函数级决策落注释

## 移除兜底模型机制（未提交）

- 用户决策：默认模型和兜底模型语义重复，彻底去掉兜底，默认模型由用户配置，报错走 SDK 默认行为
- 删 `fallbackModel` 字段（platform-presets）、候选列表兜底分支（pi-init，只取默认模型）、`trySwitchFallback`（group-session）、ProviderSettings 兜底 UI、`onFallbackUsed` IPC（preload/vite-env/ChatPanel）

## 其他

- image-vision MCP（`~/dev/project/mcp/image-vision/`，专门给 CC 用）默认模型升级 `qwen3.6-flash` → `qwen3.5-omni-plus-2026-03-15`
- 调研 deepseek-harness（`~/dev/project/GitHub/deepseek-harness`）：核心结论「everything is a plugin」+「模型可见⟺已记录」不变量 + spill 分流；缓存命中率 98-99% 主因是长会话前缀稳定（system 稳定排序 + 动态内容移出前缀 + compaction 复用请求头）
- 临时文件清理：归档「6 开源项目参考库」→ `docs/archive/`，删除识图测试 png / 草稿 / 测试文件

## 代码 review 隐患修复（23 文件改动收尾）

- review 方式：残留关键词扫描 + lint + 委派 2 个 agent 深读 ChatPanel（会话链路）/ ProjectPage（tab 联动）
- **H1** 项目切换后空白：`clearTabs()` 后补建空会话 tab（与打开 EM 初始态一致），URL 直达会话时 closeEmptyTab 清理
- **H2** onChatSession 盲匹配 → tabId 锚定：sendMessage 透传 `tabId`（preload/vite-env/ipc/agent-service 类型链路），chat-session 广播回带，前端精确绑 tab，tab 已关则丢弃（防会话孤儿/旧 tab 复活）
- **H3** sendText 重入保护：新会话首条在途（onChatSession 前）再发送直接丢弃
- **M1** openSession 命中已有 tab 时同步真实标题（会话列表点击覆盖"对话"占位）
- **M2** conv.get 异步竞态：点击序号 ref 对比，过期结果丢弃
- **M3** 角色选择器排除 existingSid（恢复会话加载期不再显示无效按钮）
- **L 组**：动画定时器 ref cleanup、roleOverride 死参数删除、临时 sid 清 runningSessions、迁移 effect 标注防御性、onContextRotated suppress 补建、App `t as Tab`
- **残留清理**：agent-service 群聊错位注释、migration 白名单 group-sessions 条目、Sidebar hasUpdate、过时注释、"规则 4b"→"规则 4"
- **历史 lint warning 清零**：api-clients/builtin-mcp/migration-service/network-service/PairRequestModal/TransferModal 未使用变量与注释（lint 0 error 0 warning）
