# 开发记录 2026-08-13

## 22. 创建项目工作流重构 & 发布 v0.8.0（2026-08-13）

> 背景：v0.8.0 从 8/10 的「创建项目对话引导设计」定稿出发，本日集中实现——创建项目从表单采集转向对话共创，配套系统提示词精简、表单采集恢复、skill 体系重构，并发布 v0.8.0。
> 定位升级：EasyMint 重新定义为**通用 Agent 软件**（围绕 Pi Coding Agent 构建），对编程小白特别优化项目引导流程。

### 22.1 创建项目对话引导（提示词 + skill 化）

- **creation_flow 提示词块**（提交 `7796541`）：创建项目走对话共创——开场一句话问意图、先草案再追问、6 场景差异化引导、成本智能引导（主动指出矛盾+取舍三选）、原型 Gate、7 道 Gate 硬约束、流程地图导航
- **「直接创建」入口**（提交 `6e7c081`）：跳过表单触发 Mint 对话引导补全信息
- **系统消息简化**（提交 `f709cac`/`2a3d8ba`/`4745228`/`d12ca0d`）：固定引导流程内置 creation_flow，消息只传动态信息；不暴露内部专用名称（creation_flow → 内置流程）；去"直到足够开始开发"冗余
- **静态 HTML 降级**（提交 `0b9d5b7`）：四处提示词不再误导静态单页创建 dev server
- **创建项目工作流 skill 化**（提交 `74679d6`）：`<creation_flow>` 精简为骨架（50→20 行），采集细节下沉 6 个 skill（creation-guide 编排 + 5 个 creation-flow-* 流程子 skill + 场景/成本数据文件）；creation 系列注册 EM_SKILLS；系统消息强指令触发引导
- **原型委派前确认 + 渲染正确性审查**（提交 `5e3910f` 前后）：出原型前先向用户复述需求确认；Mint-D 交付前代码级渲染自查（不依赖识图，防黑屏等布局问题）

### 22.2 创建项目表单（收敛后恢复完整采集）

- **表单收敛最小集**（提交 `74679d6`）：先收敛为 2 步最小集（名称/形式/完成度/部署/AI/预算）
- **恢复完整采集**（提交 `e7e28e2`/`5e3910f`）：恢复 4 步——基本信息（名称/描述/**场景下拉** 7 项/目录/形式）+ 功能清单（Mint 推荐微调）+ **UI 风格下拉**（18 项主流风格，专业名+白话解释）+ 交付方式（完成度/部署/AI/预算）
- **直接创建快照只传已填信息**（提交 `5e3910f`）：不再混入完成度/部署/AI/预算等默认值，避免 Mint 误判用户确认
- **场景映射流程深度**：商业/实际=完整，兴趣/验证/学习=轻，技术实验=极简

### 22.3 系统提示词精简 & skill 体系优化

- **系统提示词精简**（提交 `3a31a43`）：MINT_SYSTEM_PROMPT 252→~213 行，砍冗余表述（identity 比喻/系统消息重复段/委派示例/冗长描述）保留全部规则要点；run.json 格式、文档记录规范下沉为 skill（project-run/dev-docs）
- **移除冗余 skill**（提交 `3a31a43`）：easymint-guide（界面过时）、plan-first（与 CLAUDE 重复）、ask-runtime-first（触发错位，核心并入 rules）、requirement-breakdown（被 rules3 覆盖）
- **rules 查缺补漏**：需求拆解补 P0/P1/P2+待确认、自我约束补运行时优先、guide_user 去 easymint-guide 引用

### 22.4 清理与修复

- **移除 init.sh**（提交 `327aa5c`）：僵尸脚本（无执行方），环境就绪改为 Mint 按技术栈动态验证（依赖安装+构建检查）；移除 checkInitStatus 死代码
- **删除项目清理会话**（提交 `7a0c678`）：会话元数据（cache/pinned/archived/titles/types）+ 会话目录进废纸篓
- **Agent 查看面板滚动修复**（提交 `db10861`）：流式输出时用户可滚动（复刻聊天页 autoScroll + 输入时间窗判定）
- **工具结果信息价值重构**：bash 读文件等长输出截断（只显尾部+完整见日志）、edit diff 自动展开彩色展示、input JSON 不再铺开、修 `useState(!!item.result)` 重挂误展开 bug

### 22.5 定位升级 & 发布

- **README 重写**（提交 `db10861`）：定位升级为"通用 Agent 软件 + 新手引导优化"，盘点个性化功能（对话引导/原型先行/多Agent/上下文自管理/数据主权/视觉识别可配置）
- **发布 v0.8.0**（提交 `10ecd22`）：版本 0.7.3→0.8.0，tag v0.8.0，已 push origin（GitHub CI 自动 Release）
