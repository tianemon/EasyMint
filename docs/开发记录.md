# 需求跟进 #1 — AI 驱动的完整开发平台

> 本文件是项目进度与交接的**唯一真相源**（导航页）：
> - **头部「当前进度快照」**：记录当前状态（当前版本/最近工作/接下来安排），每次会话交接时更新——**本区可改**
> - **「开发记录索引」**：日期 → 日志文件映射（按日期正序，最新追加在末尾）。每天日志独立存 `docs/开发记录/<日期>.md`（只增不改，记录项目变动/用户决策/实现详录，带日期 + 提交 hash 关联，方便回溯）——按需读取，避免单文件无限膨胀
> - 正式发布日志（用户可见变更）见 `CHANGELOG.md`（Keep a Changelog 规范），本文件不重复记录
> 2026-05-28 初次建立。2026-05-29 补充 GAN 对抗评估体系。
> 2026-06-03 ~ 2026-06-21 多轮迭代，详见各次更新条目。
> 2026-06-25 更新：v0.3.0 — Skill 注入机制改造、Hook 校验、Compact 体验优化、会话历史直读 JSONL。
> 2026-07-28 更新：第五步 — Pi SDK 迁移收尾，流式输出、权限、路径修正。
> 2026-08-09 更新：头部新增「当前进度快照」（替换 CLAUDE.md 交接区的内容职责）。

---


## 当前进度快照


> 会话交接时更新本段（当前版本 / 最近工作 / 接下来安排），详细历史见下方章节与 `CHANGELOG.md`。

### 当前版本

- **v0.12.0**（2026-08-25 发布：上下文压缩修复 & 输出面板体验 & 供应商配置优化）

### 最近工作

**2026-08-25 发版 v0.12.0**（详见 `docs/开发记录/2026-08-25.md`）：
- 上下文压缩：自定义供应商模型 contextWindow 查表修复（1M 窗口不再 20% 就压缩）、回合内节流上报（修复"跳过 75%"盲区）、压缩后使用率显示"—"
- 输出面板：三个弹层可复制 + 自由滚动 + 回底按钮、shell 标题栏日志路径跳转、后台 shell 无输出真根因（Mint 命令重定向 → 提示词约束）
- 供应商配置：新增默认自定义、Base URL/协议前置、自定义排第一、默认模型下拉通用
- Pi SDK 升级 0.84.3；发版提交（git tag v0.12.0 已推送）

**2026-08-25 深夜：压缩频繁根因——自定义供应商模型 contextWindow 硬编码 200k**（详见 `docs/开发记录/2026-08-25.md`）：
- 会话 jsonl + SDK 数据实证：kimi-k3/deepseek-v4-flash 真实窗口 1M，自定义供应商注册时硬编码 200000（maxTokens 4096）→ 真实只用 ~20% 就触发 EM 弹窗/SDK 兜底
- 修复：getModelSpecLookup 跨全部 provider 数据查表（884 模型命中），命中用真实 contextWindow/maxTokens；压缩后使用率未知显示"—"（不再 0% 误导）
- 弹窗"跳过阈值"盲区：长工具回合（14 分钟连续工具调用）内上下文暴涨无上报 → 回合内节流上报（5s），75% 弹窗可在回合中触发

**2026-08-25 晚间：Shell 与子 Agent 面板体验修复 4 项**（详见 `docs/开发记录/2026-08-25.md`）：
- 自定义供应商默认模型下拉（内置/自定义通用）
- **后台 shell 无输出真根因**：Mint 后台命令自带 `> file 2>&1` 重定向绕过自动收集 → 提示词 + 工具描述明确「禁止手动重定向」+ 面板兜底提示；另修复 decodeSeg 吞前缀（混非 UTF-8 卡输出）
- 三个输出弹层（子 Agent/后台命令/运行日志）可复制 + 自由滚动 + 回底按钮
- shell 面板标题栏日志路径点击跳转文件夹（新 IPC shell:reveal-in-folder）
- 方案落盘 `docs/design/Shell与子Agent面板体验修复方案.md`；lint/构建/解码逻辑测试通过，端到端待含修复版本实测

**2026-08-25 文档治理：README 重写 & 文档规范化 & 开发记录补写**（详见 `docs/开发记录/2026-08-25.md`）：
- README 重写：开源 Harness 定位（Pi 开源非企业级）、去 Skill/MCP 过度描述与终端、徽章精简、客观描述完善功能（便签/子 Agent/迁移/Issue/会话管理）
- 创建双路径确认（对话直接创建 + 表单，提示词无冲突）；文档规范化（启动指引精简、design/ 文件夹标注、清理空目录）
- 发版流程三处明确「发布前必更新 CHANGELOG + 同步开发记录」；本次补写 08-20 ~ 08-25 日志

**2026-08-24 迁移功能重构 & 提示词治理 & 发布 v0.11.0**（详见 `docs/开发记录/2026-08-25.md`）：
- 迁移会话丢失根因修复：migration-service 硬编码旧会话目录（~/.easymint/sessions）→ getPiSessionDir 统一
- 迁移升级：单会话改多会话（会话名提取）、文件树选择器（展开/半选/全选全否默认）、全局忽略项（类 gitignore 通配符/任意层级匹配）、选中清单打包、接收端显示恢复数
- 打包资源路径修复（getResourcesDir，编辑器空白根因）；「设备互联」更名「项目迁移」
- 提示词 rules 0-10 重排 1-12 + 新增规则 12 需求理解；CLAUDE.md 需求理解规则 10-13（字面优先/位置词以用户视觉为准）
- 08-20 会话分析：66 个误解案例 → AI意图理解规则集设计（落盘 docs/design/）
- 08-23 本地 git 服务端集中化（~/git-server/，个人工作流）

**2026-08-19 会话可靠性修复（超时/压缩/错误提示）& 发布 v0.10.4**（详见 `docs/开发记录/2026-08-18.md`）：
- 超时卡会话根治：SDK 5 分钟 idle 超时 + isStreaming 残留 → steer 入队卡死；activePromptSessions 追踪 + 强制复位
- 压缩 5 问题：手动压缩桥接/使用率刷新、SDK 触发点调高（~98% 兜底）EM 弹窗主导、错误回合提示
- 双轨机制：EM 弹窗（60-80%）+ SDK 兜底（98%）

**2026-08-19 文档治理 & Skill/工具机制优化 & 发布 v0.10.3**（详见 `docs/开发记录/2026-08-18.md`）：
- dev-docs skill 扩充：重点维护 4 个文档（开发记录导航页/明细、CHANGELOG、技术架构）完整规则
- 机制梳理：skill=提示词段落按需 Read（非 tools）、MCP/内置工具=真 tools 常驻
- 4 优化：ui-sync 挂钩、工具描述下沉到本体消除漂移、rules 10 用户级 MCP 约束、skill 注入体积评估（维持现状）
- 整体逻辑检查通过（rules 0-10 连续、引用链全通）

**2026-08-19 移除请求超时 & 会话卡死修复 & 发布 v0.10.2**（详见 `docs/开发记录/2026-08-18.md`）：
- 移除 10 分钟请求超时（用户决策：SDK/API 端自处理，长思考不误掐）；修复曾因超时只 reject 不取消底层导致会话卡死（SDK 源码实证 "Agent is already processing"）
- 关于页名称修正：pi-coding-agent 包名 → Pi Agent SDK

**2026-08-18 打开/预览语义修正 & 系统通知可选 & 发布 v0.10.1**（详见 `docs/开发记录/2026-08-18.md`）：
- 「打开原型」不再触发验证流程：提示词明确打开≠验证（ui_tools + rules 9 + prototype skill），直接系统 open，不用 Playwright/服务器；Evaluator 的 Playwright 验收保留
- 系统通知卡片文字可选中复制（msg-bubble-system 恢复可选）

**2026-08-18 创建流程重新设计 & 提示词治理 & 原型编辑器修复 & 发布 v0.10.0**（详见 `docs/开发记录/2026-08-18.md`）：
- 创建流程重设计：Mint 自判复杂度（skill 严格规划）+ 打开新窗口首句问原型 + 简单项目跳过；修复 Step1 提前初始化时序错乱根因（JSONL 实证）；快速原型前置到成本之前，成本改功能共创内联校验
- 系统提示词治理（对照 cc 风格）：复杂度判定统一指向 skill、删除重复文案、口语化改精准、5 处断链修复（set_project_stage 死引用等）
- 子 agent 提示词：抽取共享 DESIGN_SPEC 常量消除 DESIGNER/BOOST 漂移；子 agent 版删交互指令
- 原型编辑器修复：blob URL 导致相对路径资源 404——注入前改写绝对路径 + 加「在浏览器中打开」按钮
- 视觉补强：模型不支持图片时提示词引导转调 describe_image
- 清理 set_project_stage / state.json 残留（v0.5.0 遗漏）；新建项目下拉越界修复

**2026-08-18 SDK 升级 0.84.2 & 自定义添加模型 & 发布 v0.9.4**（详见 `docs/开发记录/2026-08-18.md`）：
- Pi SDK 升级 0.84.1→0.84.2：模型列表大量更新（Z.AI/OpenAI/Google/DeepSeek 新增多系列），确认 glm-5.3 在 zai 和 opencode-go
- 供应商配置新增"添加模型"输入框：补充 SDK 列表外模型、合并去重、extraModels 独立字段持久化（修复编辑后丢失）

**2026-08-15 压缩链路修复（蒙版 4 层防御 + 使用率刷新）& 弹窗排版 & 呼吸灯调优 & 发布 v0.9.3**（详见 `docs/开发记录/2026-08-15.md`）：
- 压缩蒙版卡死修复：compacted 无条件广播 + 主进程 finally done + 前端 done 清蒙版 + 60s 超时兜底；压缩后主动刷新使用率
- 压缩弹窗方案 A 排版（选项集中 + 输入指令分隔区）+ 风格优化（去边框/加宽/层级统一）
- 呼吸灯 blur 1.5px 定稿（卡顿临界点内最锐利）

**2026-08-15 归档列表优化 & 抽屉统一 & Issue 面板重构 & Mint 联动 & 发布 v0.9.2**（详见 `docs/开发记录/2026-08-15.md`）：
- 归档列表：恢复按钮（悬停）+ 时间显示 + 双向实时刷新 + 入场动画；归档/结束会话自动关 tab
- 任务/运行/Issues 抽屉底色统一（--color-drawer-panel）；Issue → Issues；文件/会话列表背景一致
- Issue 面板重构：模型精简（去 symptom/notes）、编辑能力（issue:update）、记录/编辑弹窗化、行操作按钮化、去追加/待处理
- Mint 联动：list_issues 加模块 + set_issue_status 工具（修复闭环）

**2026-08-15 子 Agent 过程弹层修复 & 状态栏信号语义 & 呼吸灯 & UI 防选中 & v0.9.1 重新发布**（详见 `docs/开发记录/2026-08-15.md`）：
- 委派子 Agent 过程弹层三修：message_start 消息边界（多输出段独立气泡，修后段覆盖前段）、3s 重载兜底新旧批次共存 key 冲突（`d-uuid-N` 重复）、纯思考/工具消息开关关闭时不渲染空白气泡
- 状态栏信号语义精确化：「正在请求」= 等待 API 返回（turn_start 语义对齐 SDK 源码实证）、「正在思考」严格绑定思考块、「正在处理」中性等待态
- 角色选择器快速拖动松手回弹根因：pointerup 后 lostpointercapture 误触发 abortDrag → syncSlider 读旧 chatRole 回弹——移除该路径（日志实证落点计算正确）
- 输入卡片任务/命令胶囊雾化呼吸灯（绿/蓝、box-shadow 内外雾化、2.8s 呼吸）；侧边栏任务按钮呼吸灯移除
- UI 文字全局防选中（body none + 内容型容器恢复）
- v0.9.1 覆盖 tag 重新发布（并入以上改动）

**2026-08-14 环境变量全量导入 & dev Ctrl+C 失效根因修复 & 消息气泡粒度对齐 & 发布 v0.9.1**（详见 `docs/开发记录/2026-08-14.md`）：
- 环境变量全量导入：loadUserPath → loadUserEnv（`zsh -lc` 显式 source .zshrc 全量注入，PATH 前置合并）
- **dev Ctrl+C 失效根因**：`zsh -lic` 交互模式 tcsetpgrp 抢占终端前台进程组，zsh 被杀后前台进程组悬空 → Ctrl+C 信号发往死进程组。修复：去 `-i` 非交互提取，根治
- 项目重命名/更新路径 Pi session 目录迁移（历史会话不丢失）
- 流式消息调查（sdk.md + Proma 源码实证）：SDK 粒度 = 每条 assistant 消息独立；用户定稿保持 SDK 默认行为——撤销回合合并、磁盘渲染不再合并相邻 AI 消息
- diff 标题栏「编辑」竖排修复；诊断日志全清（5 处 [diag]）

**2026-08-14 输入卡片初始态 & 群聊移除 & 委派机制改造 & 角色选择器 & 发布 v0.9.0**（详见 `docs/开发记录/2026-08-14.md`）：
- 输入卡片初始态：打开 EM 即完整聊天页（输入卡片居中 + Agent 角色选择），tab 栏隐藏、发送首条消息后落位并显示；tab 联动抽象到 tab-store（hasRealTabs/closeEmptyTab/openSession），会话列表与 TabBar 统一入口；项目切换自动补建会话 tab
- 群聊彻底移除：group-session/GroupComposerDialog/GroupSettingsSection + group:* IPC + 协作规则提示词全删
- 委派机制核心改造：决策树改为「上下文保护 + 综合掌控」双维度（必须委派/必须亲自/按上下文重合度），委派深度=1、类型化；Mint-D 作为 Mint 能力附加（不覆盖身份）
- 移除兜底模型机制：默认模型由用户配置，报错走 SDK 默认行为
- 角色选择器重做（液态玻璃滑动分段控件）：滑块宽度跟随内容、拖拽状态机重构（三级兜底/ref 实时值/落点直接算几何）、文字双层 clip 逐字点亮、拖拽宽度边缘触发适配、气泡玻璃质感（壁线/彩虹边框/白光）
- Agent 模板设置优化（Mint 默认徽标/隐藏 max 思考/去重复文案）+ MINT_DESIGN_BOOST 放宽自由设计
- 代码 review 两轮：会话/tab 竞态修复（tabId 锚定等）+ 本轮未提交改动复查（window 监听泄漏等）
- 亮色大背景微调（--color-content/canvas 灰一档）

**2026-08-13 创建项目工作流重构 & 发布 v0.8.0**（详见 `docs/开发记录/2026-08-13.md`）：
- 创建项目从表单采集转向对话共创：creation_flow 引导 + 6 场景差异化 + 成本智能引导 + 原型 Gate + 7 Gate；细节下沉 creation-guide 系列 skill
- 表单恢复完整采集：场景下拉（7 项）/ 功能 Mint 推荐 / UI 风格下拉（18 项）/ 交付方式；直接创建快照只传已填信息，场景映射流程深度
- 系统提示词精简（252→213 行）+ 移除冗余 skill + 新增 project-run/dev-docs skill + rules 查缺补漏
- 定位升级为通用 Agent 软件（围绕 Pi Coding Agent 构建，新手引导特别优化），README 重写；发布 v0.8.0
- agentDir 双目录（`pi/` + `pi-agent/`）合并为 `~/.easymint/agent`（严格对应 Pi 默认 `~/.pi/agent` 层级，`~/.easymint` 替换 `~/.pi` 层级）；`PI_CODING_AGENT_DIR` env + `getAgentDir()` 参数统一；启动含一次性迁移（models-store.json）
- **保持 Pi SDK 默认行为**（用户决策，同日修正）：资源发现（项目 CLAUDE.md/AGENTS.md、项目 `.pi/`、SYSTEM.md 等）不做限制；settings 恢复磁盘模式（`SM.create`，去 inMemory 单例）
- **项目级目录统一 .easymint**：启动时定制本地 SDK 包 `piConfig.configDir`（官方定制点，幂等自检）——Mint 项目级 `.pi` → `<项目>/.easymint/`；pi CLI 独立 SDK 仍 `.pi`，彻底隔离（Pi TUI 的 compaction 等配置不再影响 Mint）
- **会话目录归 Pi 默认**：`~/.easymint/sessions/` → `~/.easymint/agent/sessions/`（getPiSessionDir 一处改，9 调用点跟随；启动一次性迁移，本机 6 项目会话已迁）
- 遗留全清：Claude SDK 数据（`.claude.json`/telemetry/backups/projects 会话/commands.json）、`~/.pi/`、含 key 的 `settings.json`、147 个 `__new_` 孤儿缓存
- 验证：lint 0 errors、build 通过、patch 后 Pi 运行时确认 `CONFIG_DIR_NAME=.easymint`、`agent/` 自动生成 auth.json + models-store.json

**v0.6.5**（状态指示光效 & 聊天滚动 & 会话隔离）：
- 光效三预设全 canvas 化（orbit 环绕流光 / slide 顶部滑动 / breathe 发散柔光，含 orbit 彗尾半椭圆收尖）+ 参数固定模式（仅颜色可调）+ 亮暗两套配置 + Mint 状态文本样式（单色/流光）
- 聊天回底/新消息气泡 + 滚动状态机（输入时间窗判定用户意图，程序性滚动不误判）
- 会话隔离架构级修复：status-store 按 sessionId 分片（信号栈/摘要/压缩/ctxPct），StatusBar/AgentBar/ShellBar 按会话过滤

**v0.6.6**（设备互联 & 跨设备项目迁移，54 提交）：
- 设备互联（实验性）：mDNS 手动扫描（3s 窗口）+ 蓝牙式配对（ECDH P-256 + AES-256-GCM）+ WS 连接 + 心跳 30s，**完全手动**（广播 60s / 无自动重连）
- 跨设备迁移（纯手动）：统一扫描（排除 Electron+Flutter 构建产物）→ zip 打包（archiver 8.0）→ 两阶段握手 → 完整性校验（sha256 + 文件数 + 会话落盘验证）→ 缓存目录恢复完毕才删包；完成卡片模板
- 纯手动定稿：移除 Mint 迁移 skill + MCP 迁移工具（保留设备管理工具）；工具箱「实验」标签
- Windows 修复：GBK 乱码（spawnSync + `encoding.ts` 判定）、GPU 缓存刷屏、虚拟网卡地址（同网段优先）、archiver ESM 跨平台（external 运行时 require）
- 已推送 origin（tag v0.6.6）+ mac 存档仓库

### 接下来安排

1. **冒烟验证创建项目引导**：两条入口（表单/直接创建）+ 3 个代表场景（商业/验证/学习）实测，确认 skill 触发与引导深度
2. **状态栏信息密度**（`docs/待办事项.md` 第 1 条，缓做：耗时/token/模型名显示）
3. **轮转端到端实测**：压缩 3 次触发归档+新会话接力（使用中确认）
4. **设备互联/迁移真实场景验证**：下次跨设备使用时确认全链路（配对→迁移→会话恢复→完成卡片）

**2026-08-09 文档治理**（提交 `841d570`/`09da8d1`/`6abc9ee`/`1994c59`）：
- CLAUDE.md 交接区瘦身为纯指引（索引/发布流程/规范），进度内容迁入本文件头部快照
- 本文件确立「只增不改」时间线规则（带日期+提交 hash 关联）；CHANGELOG 规范化（Keep a Changelog + [Unreleased] 区块）
- 4 个已实现方案归档 `docs/archive/`；删除重复文档（局域网协作.md，并入 memory）；修正需求文档技术栈（pi-coding-agent）
- 全局 `~/.claude/CLAUDE.md` 新增「开发工作流」章节（文档驱动/版本发布/代码质量）

### 已暂停（暂不安排，详见 `docs/待办事项.md`）

- 会话回退 rewind（第 2 条，调研已落盘）
- 群聊代码降级（第 3 条）

---


## 开发记录索引（按日期正序（最新追加在末尾），当天记录写入对应文件，只增不改）

| 日期 | 记录 |
|------|------|
| 2026-06-04 | 2026-06-04 更新 || 2026-06-06 | 2026-06-06 更新 |
| 2026-06-09 | 2026-06-09 更新 等 1 条 |
| 2026-06-15 | 1. 愿景 等 7 条 |
| 2026-06-17 | 2026-06-17 更新 |
| 2026-06-22 | 2026-06-22 ~ 2026-06-23 更新 — v0.2.1 ~ v0.2.4 等 1 条 |
| 2026-06-25 | 2026-06-25 更新 — v0.3.0 |
| 2026-07-28 | 2026-07-28 更新 — Pi SDK 迁移收尾 |
| 2026-07-31 | 9. v3 UI 全面改版（2026-07-31） |
| 2026-08-02 | 10. Task 系统重构（2026-08-02） 等 2 条 |
| 2026-08-03 | 13. Mint 系统提示词优化（2026-08-03） 等 1 条 |
| 2026-08-05 | 15. 方案收敛:Agent 模板模块 + 动态委派(2026-08-05) 等 1 条 |
| 2026-08-07 | 17. 状态指示光效全面升级 + 聊天滚动体验 + 会话隔离（2026-08-07，v0.6.5） |
| 2026-08-08 | 18. 设备互联 & 跨设备项目迁移（2026-08-08，v0.6.6） |
| 2026-08-09 | 19. EM 可用工具全貌（2026-08-09，解耦后 Mint 视角） 等 1 条 |
| 2026-08-10 | 20. Pi 配置目录体系统一 & Claude SDK 遗留清理（2026-08-10） |
| 2026-08-13 | 22. 创建项目工作流重构 & 发布 v0.8.0（2026-08-13） |
| 2026-08-14 | 23. 消息气泡渲染优化 & 上下文文件规范化 & 移除兜底模型（2026-08-14） |
| 2026-08-15 | 压缩可靠性修复 & 弹窗排版 & 呼吸灯调优（2026-08-15，v0.9.3）等 3 条 |
| 2026-08-18 | 26. 打开/预览语义修正 & 系统通知可选（2026-08-18，v0.10.1）；25. 创建流程重新设计 & 提示词治理 & 原型编辑器修复（2026-08-18，v0.10.0）；24. SDK 升级 0.84.2 & 自定义添加模型（2026-08-18，v0.9.4） |
| 2026-08-19 | 29. 会话可靠性修复（超时/压缩/错误提示）（2026-08-19，v0.10.4）；28. 文档治理 & Skill/工具机制优化（2026-08-19，v0.10.3）；27. 移除请求超时 & 会话卡死修复（2026-08-19，v0.10.2） |
| 2026-08-25 | 30. 迁移功能重构 & 提示词治理 & 发布 v0.11.0（2026-08-24）；README 重写 & 文档规范化（2026-08-25） |
