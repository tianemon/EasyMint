# Changelog

> 遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/) 规范：只记录 release 的**用户可见变更**。
> **发布前必更新**：日常变更先记入下方 `[Unreleased]`，发版时整理成本版本条目；内部开发记录（项目变动/用户决策/实现过程）见 `docs/开发记录.md`（发版时同步更新头部快照与当天日志）。

## [Unreleased]

### Added（新增）

- **Skill 管理界面升级**：设置 → 插件 → Skills 三页签（内置 / 通用 / AI 管理）——每个 skill 显示来源徽章与调用统计（次数 · 最近使用），支持查看正文、删除（AI 管理区直接删、手写区需确认）；「AI 管理」页可手动新建 skill（名称/描述/正文实时校验），并提供「允许 AI 创建与管理 skill」开关（默认关闭；开启后 AI 会话可写入 AI 管理区，与手写 skill 物理隔离、同名时手写优先）
- **AI 会话加载 skill 的统一入口（use_skill）**：Mint 会话中按名称加载 skill，记录使用统计；skill 声明 `model` 字段时会话自动切换到指定模型（不存在则忽略）；开启「允许 AI 创建与管理 skill」后，Mint 还可在会话中创建/更新/删除 AI 管理区的 skill（manage_skill）
- **Skill 注入收敛到引擎原生格式**：会话系统提示词中的 skill 列表改由 Pi 原生 `<available_skills>` 注入（含全部 EasyMint skill，含 AI 管理区），消除自定义拼接段
- **Mint 任务执行规则增强**（提示词，内部）：修改按影响半径分级投入（L0 单点直改 → L3 跨模块先对齐需求）、改前定向求证（同文件连续微调免重读）、修改-验证闭环（收尾必带验证结果）、探索假设驱动（连续 3 次未缩小范围即停机汇报）、多问题逐项对账（最终回复逐项标注解决状态）

## [v0.15.0] (2026-08-31) — 结构化提问 & 运行面板状态修复

### Added（新增）

- **Mint 结构化提问（ask_user）**：Mint 需要用户决策时弹出选择题卡片（聊天区内嵌毛玻璃卡片，宽度与输入卡片一致）——选项点选/自定义输入/跳过当前题/全部跳过，一次一题选完自动跳转、‹ › 可前后切换重选，支持级联联动（前置选择变化后续问题动态出现）；触发场景分层：新功能设计、方案调整时使用，修 bug 与简单确认用文本
- **Mint 排查规则补全**（提示词规则 13）：用户实测即真相（不怀疑用户用旧代码）、查不明原因按理想化预期重写逻辑

### Fixed（修复）

- **运行面板状态丢失**：脚本长时间运行后进程仍在、停止按钮却消失——启动成功/停止统一广播状态（此前只广播停止，状态靠前端自行拉取无自愈）；进程退出以「进程组存活」判定（父 shell 退出但子进程仍在运行时不误清状态，持续监控至真正退出）；重启消除 500ms 竞态窗口（新进程就位后才响应，重启后状态不再丢失）；状态检测以主进程拉取结果为准（消除检测窗口误报）
- **打开文件时标签栏多出「新会话」**：首页默认会话是一个空 chat tab（`sessionId` 为空）占位，标签栏在「无真实 tab」时整条隐藏，打开文件 tab 后标签栏显形，这个占位 tab 跟着一起露出——打开 file tab 时清掉该占位（与打开会话一致）
- **侧边栏「文件」页签右侧不再残留聊天页**：切到「文件」页签时右侧内容区（含标签栏）整块留白等用户点文件；切回「会话」恢复切走前的样子（原会话仍在则回原会话，占位 tab 已被清则补回首页空会话）

## [v0.14.1] (2026-08-29) — 运行 PID 修复 & 聚焦光环体系完善 & 抽屉箭头对齐

### Fixed（修复）

- **运行面板 PID 显示**：启动/重启脚本后显示「PID undefined」（字母）而非真实进程号——启动后补拉主进程状态，显示真实进程 id
- **弹窗容器聚焦光环**：输出窗口等弹窗打开时按 Shift，出现系统默认 focus 边框（并曾导致窗口圆角被压成直角）——显式 tabindex 容器清除聚焦光环/默认 outline；全局 focus ring 不再强制圆角（光环跟随元素自身圆角）
- **抽屉指示器箭头偏移**：侧边栏抽屉（任务/运行/Issues）底部小三角用写死百分比定位（12%/46%/80%），按钮宽度随文字变化导致箭头不居中——实测激活按钮中心精确对齐（切换/窗口 resize 时重新对齐）

## [v0.14.0] (2026-08-29) — 历史输入抽屉 & 输入框视觉统一

### Added（新增）

- **「历史输入」抽屉（聊天页右上角按钮）**：右侧滑出半透明玻璃抽屉，显示当前会话全部用户提问（最新在上）；顶部搜索框实时过滤（支持清空/无结果提示）；点击记录跳转到对应消息（顶部对齐 + 圆角高亮 1.5s 淡出）；点击列表项或外部区域关闭
- **输入框视觉统一**：设置页/新建 issue/编辑脚本/新建项目/设备迁移等全部表单输入框统一为矩形大圆角（12px）+ 柔和边框样式——聚焦时外观完全不变（去除全局 focus 光环与 accent 高亮边框）

### Fixed（修复）

- **TabBar 高度漂移**：空会话/打开会话两态下 TabBar 高度差 1-2px，导致聊天页顶部（含历史输入按钮）位置漂移——TabBar 高度固定 40px
- **输入框聚焦高亮残留**：全局 `*:focus-visible` 光环覆盖所有元素（含输入框）——输入框排除出全局光环规则

### Changed（优化）

- **任务面板**：去除进度条；任务列表倒序（最新任务在最前）
- **面板标题栏统一**：任务/运行/问题记录三个抽屉面板标题栏去除底部横线、缩小与内容间距

## [v0.13.0] (2026-08-28) — UI 字号统一管理 & 内容复制体系

### Added（新增）

- **「界面字体」滑杆（设置 → 外观）**：所有 UI 文字（文件列表/侧边栏/状态栏/会话列表/输入栏/设置页/弹窗）按 90%~130% 百分比统一缩放——此前 UI 骨架字号完全不受设置控制，170+ 处硬编码字号全部接入语义变量
- **「阅读字体」滑杆改为百分比**：由 1-6 级改为 90%~130% 百分比（老用户设置自动迁移），控制所有需持续阅读的动态内容——聊天消息（正文/代码块/diff/思考过程/工具折叠）、代码编辑器、Shell 输出与运行日志、子 Agent 过程输出、脚本/提示词编辑区、便签正文
- **两套字号控制源划清界限**：界面骨架（文件列表/侧边栏/状态栏/会话列表/输入栏/设置页/弹窗）归「界面字体」，动态阅读内容归「阅读字体」，互不影响

### Fixed（修复）

- **重启后字号设置不生效**：此前 `loadFromElectron()` 只恢复设置值不写 CSS 变量，改过字号后重启回落默认值，直到再拖一次滑杆才生效——启动时同步应用 CSS 变量
- **老用户聊天字号级别迁移失效**：磁盘旧 `chatFontLevel`（1-6 级）被主进程默认值吞掉，升级后字号静默回落 100%——去兜底让旧级别正确迁移（5 级 → 114%）
- **文件树内联 12px 硬编码**：`FileTreePanel` 内联 `fontSize:'12px'` 不受字号设置控制，已接入语义变量
- **环形圈百分比文字放大溢出**：UI 字号放大时 20px 圆环内 8px 百分比文字可能溢出，加 `clamp(8px, var(--text-4xs), 10px)` 限幅保护
- **输出窗口内容无法复制**：窗口合并时输出区丢失可选标记（全局防选中生效）——恢复复制；便签正文/权限命令描述/问题标题统一接入可选类
- **便签文字拖动选择失效**：置顶操作触发 React 重渲染，Chromium 交互式文本选择被回滚（松手即取消/需先点击）——置顶改为零重渲染（直写 DOM 层级）
- **Ctrl+A 归属细化**：焦点在内容区（消息气泡/输出窗口/便签/diff）只全选该容器；点击空白后二次全选不再失效

## v0.12.2 (2026-08-28) — 输出管线修复 & 窗口统一 & 交互优化（合并重发）

### Fixed（修复）

- **打断不再显示「已停止」提示**：打断是主动操作，按钮状态变化即反馈——abort 类错误静默处理；真实错误（503/429/超时）仍提示 8s
- **Mint 读 Skill 报 EISDIR**：skill 注入列表的路径原指向文件夹（如 `.../skills/project-run`），Mint 按提示 Read 文件夹报"illegal operation on a directory"——路径改为直接指向 `SKILL.md` 文件
- **子进程输出乱码（U+FFFD）**：运行面板/shell 服务日志 `chunk.toString()` 直接解码，多字节中文被 chunk 边界切断产生 `��`——统一接入流式解码（decodeSeg，UTF-8 跨 chunk 等待/GBK 兜底）
- **ANSI 转义序列残留**：彩色命令输出（flutter 构建等）的 `[0;32m` 原文显示——显示层保留 ANSI 转彩色渲染；喂模型的文本（前台 bash/后台完成通知）剥离
- **链接点击窗口内跳走**：Mint 回复的网址点击后 EM 界面被替换无法返回——`will-navigate` 拦截外部 URL 转系统浏览器 + marked 链接加 `target="_blank"`
- **打开已有会话体验**：输入卡片先渲染、使用率按钮加载期间填充旧进度误导——消息加载期间显示「正在加载会话…」占位，使用率延迟到加载完成恢复
- **Ctrl+A 全选整个页面**：焦点在消息气泡内只全选该气泡；空白区域禁止页面全选；输入框保持编辑语义
- **弹窗点击关闭误触下层**：点击运行日志窗口误关侧边栏抽屉——全局弹窗分层检测（overlay-stack），点击上层弹窗不关闭下层
- **输出窗口拖拽选中移出边缘误关窗口**：遮罩关闭改为按下+松开双判断（onClick 公共祖先语义导致）

### Changed（变更）

- **运行日志/后台 shell 窗口合并**：统一 OutputWindow 组件（功能并集：停止按钮/日志路径/截断提示/ANSI 彩色/自动滚动/Ctrl+A/遮罩双判断）——两处入口同一弹窗；子 Agent 过程弹窗同步统一尺寸（80vw×80vh）与视觉
- **日志面板 ANSI 彩色渲染**：One Dark 配色，日志内容先 HTML 转义防注入
- `buildSkillsPrompt` 三组 skill 输出封装为 `pushSkillGroup`；`scanSkills` builtin 归并合并（行为不变）
- vite.config `__dirname` → `import.meta.dirname`（Vite 8 native configLoader 兼容）

## v0.12.1 (2026-08-27) — 打断状态机修复

### Fixed（修复）

- **打断后按钮卡在打断态（busy 残留）**：多层时序竞态修复——
  - 打断时 SDK 抛 AbortError 被误报为英文错误 → 归一化为「已停止」（短显示 2s）
  - SDK 错误回合（turn_start → error → exit）turn_start 设的 busy 残留 → error 事件清 busy
  - 打断后旧回合 exit 误清新回合 busy（后台通知恰在打断时开回合）→ 打断 1.5s 窗口忽略旧 exit
  - error 后残留事件（tool_result/message_end）把 busy 打回 → error 后 1s 内不重新设 busy
  - 打断后「已停止」消失回退显示残留 bash 执行信号（sleep 90）→ error 清 tool 信号

### Changed（变更）

- vite.config.ts `__dirname` → `import.meta.dirname`（Vite 8 native configLoader 兼容）+ 合并重复 build 键

## v0.12.0 (2026-08-27) — 运行面板脚本管理

### Added（新增）

- **运行面板升级为脚本管理器**：不只启动项目——Mint 写的常用脚本（运行/构建/打包/安装/发版部署等）都加入面板一键运行；Mint 写脚本后询问是否添加或长期复用脚本默认添加并告知；项目完成时按技术栈生成预设脚本
- **脚本标题可编辑 + 命令引用脚本文件时跳转编辑器**：点击标题打开编辑弹窗（标题+命令极简表单）；命令调用脚本文件（bash/./xx.sh 等）时提供「编辑脚本文件」按钮，直接用 EM 编辑器打开文件本体
- **平台标签按命令自适应**：按「解释器+文件扩展名」（bash xx.py→python、./xx.java→java、./xx.js→nodejs）或命令首词（flutter/git/npm…）推断；未命中小众命令直接显示命令首词——不再出现 sh 脚本显示 flutter 的情况；新增 git/java 平台标签
- **run.json 写能力**：编辑/删除脚本写回 run.json（保留原条目未知字段与顶层结构，兼容旧版格式）
- **Issues 记录/编辑弹窗升级**：全窗口居中 760×600（此前 440px 且被侧边栏 transform 劫持无法真正居中）

### Changed（变更）

- 脚本卡片布局：标题 hover 滚动完整显示（点击可编辑）；平台标签移至标题下方；命令不再常显
- 删除按钮：未运行态显示（内联确认），与运行按钮等高并排
- 抽屉高度 360px → 510px（运行/任务/Issues 统一）；启动按钮文案「运行」
- 编辑弹窗 760×600 全窗口居中（createPortal 挂 body 脱离抽屉劫持）

### Fixed（修复）

- 脚本编辑弹窗此前被侧边栏 transform 劫持无法全窗口居中（createPortal 修复）
- saveRunJson 兼容旧版 run.json（保留未知字段，仅覆盖已知字段）

### Changed（依赖集体更新）

- 16 个依赖升级（Wanted 范围）：vite 8.1.5→8.2.2、electron 43.2.0→43.4.1、eslint 10.8→10.9.1、zustand 5.0.14→5.0.15、vitest 4.1.10→4.1.11、@vitejs/plugin-react 6.0.4→6.1.0 等（electron 44 / typescript 7 为 major 暂不升级，保持稳定）

## v0.11.4 (2026-08-27) — 供应商切换细节修复 & 配置页按钮优化

### Fixed（修复）

- **切换供应商后模型下拉不跟随**：切换供应商后聊天页模型选择仍显示上一供应商的模型，需手动重选——主会话模型下拉改为跟随全局默认模型（store.model 联动 chatModel），切换即更新
- **供应商配置页保存/取消按钮不可见**：表单较长时按钮在滚动区外，用户误点设置页 Footer「保存」导致编辑丢失——保存/取消改为 sticky 底部条始终可见；Footer「保存」改名「完成」（语义=关闭窗口）

## v0.11.3 (2026-08-27) — 会话级供应商切换 & 工具结果显示修复

### Added（新增）

- **会话级供应商切换**：设置中切换供应商后，当前会话同步热切到新供应商（无需新建会话）；会话绑定持久化，重启/重开会话仍沿用绑定供应商。此前切换只对新会话生效，已存在会话继续用创建时的供应商
- **write 工具结果显示写入内容**：write 写文件后展示带行号的完整内容预览（与 edit 的 diff 区分），不再只有标题栏路径

### Fixed（修复）

- **工具结果开关失效（mcp 结果仍显示）**：关闭「工具结果」后 mcp_tavily 等搜索结果仍渲染到页面——工具结果改为跟随开关，仅 edit/read/write/bash 的开发结果保留（diff/路径/命令摘要）
- **日志窗口标题栏挤压**：窗口较窄或命令较长时，状态文字/按钮被压缩换行成竖排——label 截断占主空间，状态与按钮固定不收缩
- **AI 回复中粘贴图片附件条放大**：busy 时附件预览条使用 64px 大缩略图版（非 busy 为 16px 胶囊），视觉突兀且超宽——统一为胶囊式，两处视觉一致

## v0.11.2 (2026-08-26) — 压缩弹窗修复

### Fixed（修复）

- **压缩弹窗 75% 阈值失效**：防重复弹窗标记在会话切换/弹窗关闭/压缩失败后残留，导致阈值到了不再弹窗——压缩完成、会话切换时重置，弹窗关闭后下次回复重新武装
- **压缩蒙版误导显示**：压缩蒙版此前在 SDK 真正开始压缩前就显示（手动路径预广播）——SDK 未开始压缩（如卡在中断等待）时界面显示"正在整理"但实际没压缩。改为显示严格跟随 SDK 真实状态（compaction_start），SDK 未开始则不显示
- **压缩状态异常卡界面**：压缩挂起时输入永久禁用，用户只能结束会话——120 秒后自动恢复界面并提示（不中断压缩，可能仍在后台）

## v0.11.1 (2026-08-25) — 上下文压缩修复 & 输出面板与供应商配置优化

### Fixed（修复）

- **自定义供应商模型上下文窗口错误（压缩过早）**：自定义供应商注册模型时 contextWindow 硬编码 200k（maxTokens 硬编码 4096）——1M 窗口模型（kimi-k3/deepseek-v4-flash 等）只用了 ~20% 就触发 EM 弹窗压缩和 SDK 自动压缩。改为从 SDK 全量模型数据查表取真实窗口/输出上限
- **压缩弹窗错过阈值（长回合内无上报）**：上下文使用率只在回合结束时上报——长工具回合（连续几十次工具调用）内上下文暴涨时，EM 弹窗在 75% 无触发机会，直接由 SDK 兜底压缩。改为回合内节流上报（5s），弹窗可在回合中触发
- **压缩后使用率显示 0%**：压缩完成且尚无新回复时使用率未知，原显示 0% 误导——改为显示"—"（未知），下条回复后恢复真实值
- **后台 shell 面板无输出（真根因）**：Mint 后台命令习惯性手动重定向输出（如 `> file 2>&1`），绕过系统自动收集——输出面板永远"等待输出"、日志文件为空。系统提示词 + bash 工具描述明确「后台命令输出自动收集，禁止手动重定向」，面板空输出时提示可能原因
- **后台 shell 输出解码健壮性**：输出混入非 UTF-8 字节时解码器吞掉已确定内容导致输出卡住——已确定前缀即时输出，仅保留不完整尾部等待
- **自定义供应商默认模型缺失**：编辑自定义供应商时无法指定默认模型（此前仅内置供应商可选）——配置页补全

### Changed（优化）

- **Pi SDK 升级 0.84.3**：模型数据更新（GLM-4.6V、deepseek-v4-pro-0813 等新模型自动进查表）、压缩失败扩展事件（session_compact_failed）、Windows PowerShell 工具
- **输出面板可复制 + 自由滚动**：子 Agent 过程、后台命令、运行日志三个弹层内容可选中复制；流式输出时用户滚动即停止自动贴底、可自由查看历史，滚离底部显示「回到底部」按钮恢复跟随
- **后台命令日志文件快捷跳转**：shell 输出面板标题栏显示日志文件路径，点击在文件夹中显示（不打开文件）
- **供应商配置页优化**：新增供应商默认选中「自定义供应商」（平台下拉自定义排第一）；Base URL + API 协议移至 API Key 前

## v0.11.0 (2026-08-24) — 项目迁移重构：文件树选择器 & 全局忽略配置 & 会话修复

### Added（新增）

- **迁移文件树选择器**：迁移前按项目根目录展示标准文件树——文件夹可展开、勾选联动（半选态）、节点显示大小、文件夹显示「N 个文件 · 总大小」，支持全选/全否/默认、展开全部/折叠；被忽略文件可见但默认不勾选，可手动勾选任意文件
- **会话多选迁移**：会话列表显示会话名称（从会话内容提取）与时间，默认勾选最新会话，可多选历史会话一并迁移（此前仅迁最新 1 个）
- **迁移忽略项（全局配置）**：类似 .gitignore 的全局忽略名单（`~/.easymint/migration-ignore`），支持 `*` 通配符，内置常见框架/语言构建产物、敏感文件等排除项，可在项目迁移面板直接编辑保存或恢复默认
- **「设备互联」更名为「项目迁移」**：入口与面板文案统一，发送按钮更名「迁移」

### Fixed（修复）

- **迁移后会话丢失**：迁移服务仍读旧会话目录（`~/.easymint/sessions`），v0.7.2 已迁移到 `~/.easymint/agent/sessions`——修复后会话正确打包迁移
- **安装版 HTML 编辑器空白**：打包后资源路径解析错误（`__dirname` 在 asar 内无法到达 extraResources）——统一 `getResourcesDir()` 修复，内置 skills/原型模板/品牌库路径一并修复
- **嵌套目录排除失效**：忽略规则只匹配项目根级，`app/renderer/dist`、monorepo 子目录 `node_modules` 等排除不到——升级为 gitignore 语义的任意层级匹配

### Changed（变更）

- **系统提示词规则重排**：规则编号 0-10 重排为 1-12，新增「需求理解」规则（对象候选/层级确认/方向词复述/语境继承/一次一变量/证据优先/验证生效等 12 项，沉淀自真实会话失败案例）
- **迁移接收端显示恢复数量**：完成卡片与弹窗显示「已恢复 N 个会话」

## v0.10.4 (2026-08-19) — 会话可靠性修复（超时/压缩/错误提示）

### Fixed（修复）

- **超时/中断后消息发不出**：SDK 超时中断后会话状态残留导致再发消息无响应、不请求 API——steer/sendMessage 检测残留自动复位，恢复正常发送
- **长思考不再被误掐断**：识别并修复 SDK 5 分钟 HTTP 空闲超时对长思考的误中断
- **API 错误不再静默**：额度用完/429/529 等请求失败时明确提示「API 请求失败」，不再无声无息
- **手动压缩后使用率不刷新**：手动压缩时 SDK 压缩事件未桥接前端，压缩后上下文百分比残留旧值——补齐桥接，压缩后正确刷新
- **SDK 自动压缩不再绕过确认**：SDK 自动压缩触发点调高到接近满时兜底，日常压缩由确认弹窗主导（避免无提示直接压缩）
- **压缩蒙版文案准确化**：「Mint 正在总结对话」→「正在整理上下文」（压缩不等于总结）

## v0.10.3 (2026-08-19) — 文档治理 & Skill/工具机制优化

### Added（新增）

- **文档记录规范细化**：Mint 维护项目文档时按更完整规则执行——`docs/开发记录.md`（导航页：头部快照+索引）、`docs/开发记录/<日期>.md`（按日期明细，只增不改）、`CHANGELOG.md`（发版日志）、`docs/技术架构.md`（架构只追加），会话结束自动更新快照

### Changed（变更）

- **Skill 触发更可靠**：关键流程 skill 显式挂钩触发时机（如新需求 → ui-sync 检查 UI 同步），不再只依赖语义判断
- **工具行为约束下沉到工具定义**：模型不支持图片时必用 describe_image、打开原型≠验证渲染、确认开发就绪标准等，统一写在工具描述中（子 Agent/恢复会话同样生效），设置页工具说明精简为触发时机索引

### Fixed（修复）

- **用户级 MCP 工具使用边界**：浏览器控制（Playwright 等）仅用于验收或用户明确要求模拟点击时，不做打开预览——避免误用（如打开原型被 Playwright 抢占）

## v0.10.2 (2026-08-19) — 移除请求超时限制 & 会话卡死修复 & 名称修正

### Fixed（修复）

- **长思考任务不再被掐断**：移除 10 分钟请求超时限制——SDK/API 端自处理，复杂任务（长思考/长时间输出）不再因超时中断
- **超时/中断后会话卡死**：曾因超时只终止等待、未取消底层请求，导致会话永久卡住（消息发不出去、不落盘、不调 API）——现在中断由用户主动打断触发，打断按钮正常收尾会话
- **「关于」页技术栈名称修正**：pi-coding-agent 包名 → Pi Agent SDK（官方产品名）

## v0.10.1 (2026-08-18) — 打开/预览语义修正 & 系统通知文字可选

### Fixed（修复）

- **「打开原型」不再触发验证流程**：用户说「打开/预览/看看效果」时直接系统打开（open/start/xdg-open），不再用 Playwright 渲染截图、不起 http 服务器——打开与验证（渲染正确性审查）语义分离，审查只从代码推理完成
- **系统通知文字可复制**：系统通知卡片（委派完成/项目创建/流程指令等）文字可选中复制，与用户/Agent 气泡一致

## v0.10.0 (2026-08-18) — 创建流程重新设计 & 系统提示词治理 & 原型编辑器修复

### Added（新增）

- **创建流程重新设计**：打开新窗口后 Mint 第一句话就问「是否根据当前的功能需求先产出UI原型？」——先出原型预览、根据原型调整需求和效果，确认后再写完整文档、进入开发；简单/极简/无 UI 项目跳过原型直接进入开发
- **复杂度判定权交给 Mint**：不再由前端按场景硬编码流程深度——Mint 收到项目信息后按项目实际内容（文件数/功能点/有无 UI/后端数据库）自行判定极简/简单/中等，判定标准严格写入 skill
- **原型编辑器「在浏览器中打开」按钮**：编辑器内一键用系统浏览器打开原型，图片/样式基于文件目录正常解析

### Changed（变更）

- **创建流程顺序修正**：快速原型前置到设备+成本之前（与 Gate 顺序一致——原型确认后功能需求/架构才可能变化，成本基于确认后的范围计算）；成本从独立步骤改为功能共创内联校验（表单已采集部署/AI/预算，无冲突默认跳过，冲突才介入）
- **创建初始化时序修复**：Step1 后 Mint 只回复「已确认」不再提前初始化（此前会读 skill/检查环境/写文档，导致后续流程时序混乱、系统消息错位）
- **系统提示词治理**：按「系统提示词=通用规则、具体场景=skill」原则去重——复杂度判定统一指向 skill、删除与 skill 重复的双因子/五步旅程/流程决策文案、口语化表述改精准（参考 Claude Code 提示词风格）
- **Skill 体系整理**：ui-sync 移除已删除的 set_project_stage 工具引用；done/failed 任务状态统一由委派结果自动回写；委派 Mint-D 与 Mint 设计模式共享同一份设计规范（消除两份漂移）；新建项目下拉菜单视口内自适应
- **视觉补强提示词**：模型不支持图片时（报错信息因模型而异），明确引导改用 describe_image 获取文字描述，识别失败如实告知不静默

### Fixed（修复）

- **原型编辑器图片不显示**：编辑器用 blob URL 加载原型导致相对路径资源（assets/ 图片等）解析失败——注入前改写为基于原型目录的绝对路径
- **新建项目「项目形式」下拉最后一行超出屏幕**：下拉菜单改为视口内自适应（底部放不下时向上展开）
- **项目创建流程系统消息时序混乱**：Step1 提前初始化导致「功能推荐」「项目创建完毕」系统消息被 Mint 的初始化回复掩盖
- 清理 set_project_stage / state.json 残留（v0.5.0 删鱼骨图时遗漏的文档死链）

## v0.9.4 (2026-08-18) — 模型列表更新 & 自定义添加模型

### Added（新增）

- **供应商配置支持手动添加模型**：内置供应商模型下拉下方可输入模型 ID（如最新的 glm-5.3）补充到列表，SDK 升级自带同样模型时自动去重；持久化独立字段，重新编辑不丢失

### Changed（变更）

- **Pi SDK 升级 0.84.1 → 0.84.2**：内置模型列表大幅更新——Z.AI 新增 glm-5.3、opencode-go 增 glm-5.3、DeepSeek V4 系列、OpenAI GPT-5.3/5.4/5.5/5.6 系列、Google Gemini 3.5/3.6 与 Gemma 4 等

## v0.9.3 (2026-08-15) — 压缩可靠性修复 & 弹窗排版优化

### Fixed（修复）

- **压缩蒙版卡死不消失**：压缩失败/中止时 `compacted` 事件不广播导致前端蒙版残留——多层防御：主进程压缩无论成败广播完成信号、事件桥接无条件广播、前端 60s 超时兜底
- **压缩后使用率残留旧值**：压缩完成时主动刷新上下文使用率（压缩后归零，下条回复后真实值）
- 压缩失败时显示错误提示（原静默无反馈）

### Changed（变更）

- **压缩确认弹窗排版**：三个选项集中排列，输入压缩指令移至分隔线后的独立区域；选项按钮去边框、弹窗加宽、层级统一
- 任务/命令胶囊呼吸灯微调（blur 1.5px 锐利度与性能平衡）

## v0.9.2 (2026-08-15) — 归档列表优化 & Issue 面板重构 & Mint 联动补全

### Added（新增）

- **归档会话「恢复」按钮**：归档列表行悬停显示，点击立即恢复到会话列表（实时刷新）；归档/结束会话自动关闭对应 tab
- **Issue 编辑能力**：行内「编辑」按钮 → 弹窗编辑问题（模块/现象）；记录问题按钮移至标题栏右上角（圆形加号图标）
- **Issues ↔ Mint 修复闭环**：新增 set_issue_status 工具——Mint 确认问题已解决后直接标记「已修复」；list_issues 输出附带功能模块

### Changed（变更）

- **归档列表 UI**：浮层底色 + 行圆角块 + 标题/归档时间布局 + 面板入场动画
- **任务/运行/Issues 抽屉面板底色统一**（更沉的灰调）；Issues 按钮文案
- **Issue 面板重构**：记录/编辑弹窗化（大输入框、无蒙版）、去掉追加备注功能、行操作按钮化（编辑/标记已修复/删除并列）、去掉"N 待处理"
- 文件列表与会话列表背景一致（暗色修复）

### Removed（移除）

- Issue 追加备注功能（notes 字段与 appendNote 全链路）

## v0.9.1 (2026-08-15) — 环境变量全量导入 & Ctrl+C 失效修复 & 气泡粒度对齐

### Added（新增）

- **环境变量全量导入**：启动时提取用户 shell 完整环境（PATH/JAVA_HOME/HOMEBREW 镜像等），供 bash/运行面板/环境检查继承，无需逐个补变量
- **输入卡片任务/命令胶囊雾化呼吸灯**：委派执行中「任务•N」按钮周围一圈绿色雾化光晕呼吸（2.8s），「命令•N」蓝色同款；侧边栏任务按钮呼吸灯移除

### Changed（变更）

- **消息气泡粒度对齐 SDK 落盘**：实时与历史渲染均为每条 assistant 消息独立气泡（对齐官方 GUI 粒度），不再合并相邻 AI 消息；委派子 Agent 过程弹层同步对齐
- **状态栏信号语义精确化**：「正在请求」= 请求发出后等待 API 返回（turn_start 语义对齐 SDK 时序），「正在思考」严格绑定思考块，「正在处理」= 工具完成后的中性等待态
- **UI 文字全局防选中**：按钮/胶囊/标签/侧边栏等组件文字不可拖选（拖动滑块不再误选文字），消息气泡/输入框/代码/diff 内容保持可选

### Fixed（修复）

- **dev 模式 Ctrl+C 无法退出**：交互式 zsh 提取环境变量时会抢占终端前台进程组，退出后 Ctrl+C 信号发往死进程组失效——改为非交互模式提取，根治
- 项目重命名/修改路径后历史会话不可见：Pi SDK 会话目录随项目路径迁移
- diff 块标题栏「编辑」文字竖排
- **委派子 Agent 实时显示**：多输出段按消息独立气泡（修复后段输出覆盖前段内容的实时显示错误）；3s 重载兜底新旧批次消息共存导致的 React key 冲突；纯思考/纯工具消息在开关关闭时不再渲染空白气泡
- **角色选择器快速拖动松手回弹**：pointerup 后浏览器释放 capture 误触发 lostpointercapture 回弹逻辑——移除该路径，落点选中恢复正常

## v0.9.0 (2026-08-14) — 输入卡片初始态 & 委派机制改造 & 角色选择器

### Added（新增）

- **打开 EM 即聊天页**：默认显示完整聊天页面（输入卡片居中 + Agent 角色选择），tab 栏隐藏；发送首条消息后输入卡片落位、tab 栏出现；关闭全部 tab 自动回到默认页；切换项目后自动补建会话 tab
- **角色选择器滑动切换（液态玻璃质感）**：滑块宽度跟随选项内容、可拖拽切换（拖拽中文字逐字点亮选中态）、按下鼓出 + 鼠标弹性拉伸；Mint 默认 / Mint-D 增强 UI 设计
- **Mint-D 自由设计**：模板覆盖不到的场景（聊天/电商/内容阅读/个人展示等）允许从零设计，设计规范与交付自查清单保留

### Changed（变更）

- **委派机制核心改造**：决策树从「任务复杂度」改为「上下文保护 + 综合掌控」双维度——大量内容/海量中间输出必须委派（保护主会话上下文）、方案综合/需求理解必须亲自（理解不能外包）、按上下文重合度分场景；委派深度 = 1、任务类型化
- **Mint-D 定位调整**：设计能力作为 Mint 主会话的附加增强（不覆盖身份），会话统一
- **移除兜底模型机制**：默认模型由用户配置，模型不可用报错走 SDK 默认行为（重试/提示）
- **Agent 模板设置优化**：Mint 标注「默认」、隐藏 max 思考等级与重复角色文案、新建模板按钮不换行

### Removed（移除）

- **群聊功能彻底移除**：群聊会话服务、协作 UI、群聊工具与协作规则提示词全部删除
- **兜底模型（fallbackModel）全链路删除**：设置界面、SDK 初始化、切换逻辑

### Fixed（修复）

- 会话与 tab 竞态：发送中切 tab/关 tab 的会话错绑（tabId 锚定）、首条消息发送窗口重入、会话列表点击异步竞态（防乱序激活）
- 滑块拖拽状态机：意外松手卡在抓取态（pointercancel/lostpointercapture/窗口级三级兜底）、拖拽不跟手（ref 实时值）、快速反向拖动的宽度残留（落点直接算几何）

## v0.8.1 (2026-08-14) — 消息气泡渲染优化 & 上下文文件规范化

### Changed（变更）

- **消息气泡折叠块统一**：思考/工具/代码块内外边距与标题栏字号统一为语义档位（body/detail/caption/code/meta），代码块标题栏空隙修复、markdown 一级标题字号贴合聊天语境
- **diff 结果默认展开**：文件修改 diff 始终默认展示、可手动收起；多工具同组时含 diff 自动展开；关闭工具调用显示时行为一致
- **上下文文件规范化为 AGENTS.md**：新项目模板与系统提示词由 CLAUDE.md 统一为 AGENTS.md（Pi/Codex 等通用 agent 兼容）；项目根存在 CLAUDE.md 时自动补读
- **文档记录规范统一**：开发进度导航页统一命名为「开发记录.md」（头部快照 + 按日期分文件的明细）

## v0.8.0 (2026-08-13) — 创建项目对话引导 & 通用 Agent 定位

### Added（新增）

- **Mint 创建项目对话引导**：创建项目从表单采集转为对话共创——开场一句话问意图、先草案再追问、6 场景差异化引导、成本智能引导（主动指出矛盾+取舍三选）、原型 Gate（中等以上必经原型确认后才开发）、7 道 Gate 硬约束、流程地图导航（方案见 `docs/design/创建项目对话引导设计.md`）
- **创建项目表单完整采集**：恢复 4 步表单——场景下拉（自己用/做成产品/验证想法/做着玩/学习/技术实验，白话点选）、功能清单（可让 Mint 推荐微调）、UI 风格下拉（18 项主流风格，专业名+白话解释）、交付方式（完成度/部署/AI/预算）
- **原型委派前确认**：出原型前先向用户复述需求确认，再委派设计师，避免原型做偏

### Changed（变更）

- **定位升级为通用 Agent 软件**：围绕 Pi Coding Agent 构建——对程序员是通用 Agent 工作台，对编程小白特别优化了项目引导流程（README 重写）
- **系统提示词精简**：252 行减到 ~213 行，砍冗余表述、保留全部规则要点；run.json 格式、文档记录规范下沉为 Skill
- **Skill 体系优化**：新增 creation-guide 引导系列、project-run、dev-docs；移除过时/冗余 Skill（easymint-guide、plan-first、requirement-breakdown、ask-runtime-first）
- **删除项目更干净**：删除项目时自动清理会话与缓存（会话目录进废纸篓）

### Fixed（修复）

- **Agent 查看面板滚动**：流式输出时用户可自由滚动查看历史，不再锁定底部
- **直接创建快照**：只传用户已填信息，不再混入默认值导致 Mint 误判用户确认
- **原型渲染正确性**：Mint-D 交付前代码级渲染自查，防止布局问题（黑屏等）漏过

### Removed（移除）

- 移除 init.sh 环境初始化脚本——环境就绪改为 Mint 按技术栈动态验证（依赖安装 + 构建检查）

## v0.7.3 (2026-08-10) — 视觉配置化 & 费用修正 & 更新提醒优化

### Added（新增）

- **视觉识别可配置化**：设置页可配置 API 地址（OpenAI 兼容 / Anthropic 兼容双模式）、模型与 Key——支持任意 OpenAI 兼容视觉服务（含阿里百炼专属实例）；内建视觉模型升级 qwen3.7-flash
- **上下文圆圈交互**：悬浮显示上下文使用百分比，点击打开会话统计弹窗（token 明细 + 费用）
- **更新提醒整体优化**：消费式红点（设置按钮 / 关于页标题，按版本记已读）+ 下载完成显示「重启升级」按钮 + 检查防重入（自动与手动碰巧只查一次）+ 下载状态持久化（重启后提醒不丢，安装后自动清除）

### Fixed（修复）

- **费用统计修正**：Pi SDK 费用统一为美元——磁盘会话费用从恒 0 改为累加每条消息 usage.cost；前端删除按模型名猜币种（DeepSeek 美元被当人民币显示的 bug），统一按央行中间价 6.7884 换算
- **模型框宽度**：90px → 160px，长模型名不再截断

### Changed（变更）

- 暗色主题配色精调：浮层/菜单/弹窗统一输入卡片色、用户气泡/tab 底色/侧边栏配色统一
- 输入卡片底部布局：主题/工具箱按钮移到侧边栏右侧，工具箱换 Phosphor briefcase 图标
- 移除旧「会话统计」按钮（圆圈已成为唯一统计入口）

## v0.7.2 (2026-08-10) — Pi 配置目录体系统一 & 消息可靠性 & 暗色主题

### Changed（变更）

- **Pi 配置目录体系统一**：agentDir 双目录（`pi/` + `pi-agent/`）合并为 `~/.easymint/agent`（严格对应 Pi 默认的 `~/.pi/agent` 层级，不再有 `.easymint/pi` 子目录）；会话数据仍存 `~/.easymint/sessions/`
- **保持 Pi SDK 默认行为**：资源发现（项目 CLAUDE.md/AGENTS.md、项目 `.pi/` 目录、SYSTEM.md 等）不做限制，Pi 怎么读就怎么读；settings 恢复磁盘模式（Pi 默认落盘 `agentDir/settings.json`，与 `em-settings.json` 职责分离不冲突）；EM 仅在 Pi 默认行为之上扩展（Mint 提示词 + skills + env/profile）
- **项目级目录统一为 `.easymint`**：启动时定制本地 SDK 包的 `piConfig.configDir`（官方定制点，幂等自检）——Mint 的项目级配置目录从 `.pi` 变为 `<项目>/.easymint/`；pi CLI/TUI 用独立 SDK 仍 `.pi`，两边彻底隔离互不干扰
- **会话目录归 Pi 默认**：`~/.easymint/sessions/` → `~/.easymint/agent/sessions/`（Pi 默认布局，启动时自动迁移旧数据，会话列表无缝保留）
- **清理 Claude SDK 遗留数据**：删除 Claude SDK 产生的文件与目录（`~/.easymint/.claude.json`、telemetry/、backups/、旧项目会话目录、commands.json）与空壳目录（logs/、shell-snapshots/、session-env/、旧日志）、`~/.pi/` 默认目录
- **暗色主题整体提亮**：背景/文字/边框提亮一档，用户气泡/tab 底色/侧边栏/输入卡片配色精调统一

### Added（新增）

- **Qwen Token Plan Individual 供应商**：设置页可选通义千问国际版订阅（配置 `QWEN_TOKEN_PLAN_API_KEY` 即可用）；供应商展示表重构为单一权威表（新增供应商只需改一处）

### Fixed（修复）

- **消息发送可靠性**：会话空闲时插话消息不再"发出去了没反应"（steer 空闲改走正常发送，根治 SDK 队列永不消费问题）；切换思考等级不再有并发竞态窗口（等级统一随发送应用）；上下文整理（压缩）期间发送有明确提示"正在整理上下文，请稍候再试"
- **Pi SDK 升级 0.83 → 0.84.1**：底层协议变更（message_update 增量）对 EM 无影响，白捡 Agent.reset() 运行中保护修复

## v0.7.1 (2026-08-09) — 会话状态管理 & 用户文案友好化

### Added（新增）

- **会话状态点**：会话列表标题前的小点显示激活状态——绿点=会话在线（内存活跃），灰白点=未激活，一目了然
- **右键「结束会话」**：会话列表右键菜单新增——点击立即结束会话（不做延迟判断）
- **tab 关闭会话回收**：关闭 tab 后会话保留 2 分钟（快速重开秒恢复），2 分钟后自动回收内存；重开 tab 自动取消回收
- **切换供应商自动热切**：设置里切换供应商后，已打开的会话自动切换到新供应商默认模型（无需手动选模型或重开会话）
- **tab 关闭提醒三态**：关闭 tab 时若 Mint 思考中/任务执行中/后台命令运行中，按实际状态提醒（"关闭将中断任务"等）

### Fixed（修复）

- 修复委派/后台命令状态残留——所有 tab 关闭后重开，光效/胶囊显示假活跃（订阅移全局常驻）
- 修复删除活跃会话后仍在后台运行（删除前先结束内存会话）
- 修复 tab 切换时会话列表选中态不跟随
- 修复切换模型时选中非默认模型误走重置（按所选模型直接热切）
- 修复长命令前台执行阻塞回合（提示词补后台执行规则，数分钟内可前台）

### Changed（变更）

- **用户可见文案友好化**：去除开发术语（子 Agent→AI 助手/任务、Shell→命令、注入会话→显示在对话里），胶囊/浮层/提示全部产品语言
- 会话右键菜单样式优化：紧凑精致（宽度 96px、内缩圆角、lucide 电源图标、危险操作红色）

## v0.7.0 (2026-08-09) — 与 Claude Code 解耦 & 运行面板链路 & UI 体验优化

### Added（新增）

- **任务按钮呼吸灯**：Agent 实际执行任务时（任务行转圈），侧边栏「任务」按钮底色呼吸提醒（亮色浅绿/暗色灰白）
- **运行面板 run.json 自动刷新**：Mint 写入/修改 run.json 后面板自动重新检测，无需手动刷新；提示词明确 run.json 用途与 platform 值域
- **用户 PATH 注入**：GUI 启动时提取 shell 环境完整 PATH——运行面板的 flutter/node 等命令可直接运行（根治 GUI 最小 PATH 问题）
- **内置模板权限体系**：Mint/Mint-D 完全锁定（只读浏览）；Builder/Evaluator 仅可调整供应商/模型/思考等级；浏览表单统一（含系统提示词只读展示）
- **Mint 文档记录规范**：进度快照 + 按日期分文件 + 归档（用户项目文档体系，长周期 token 友好）
- **Mint 工具指引补全**：describe_image / web_fetch / show_prototype / list_issues 调用时机

### Fixed（修复）

- 修复确认开发/新建项目按钮失效——Mint 回合中弹出被 busy 条件吞掉（改为广播直连，点击一次即消失，打断不复活）
- 修复设计会话加载 Mint 模板而非 Mint-D（isDesigner 传递错配，多新 tab 场景）
- 修复新建项目下拉框点开收不回（mousedown 未排除触发器）
- 修复控制台弹窗被限制在运行面板内（抽屉 transform 劫持 fixed 定位，改 portal）
- 修复快速命令结束后控制台"等待输出"（进程退出日志被覆盖清空）
- 修复 Shell 胶囊亮色主题无背景（@theme 缺 info-soft token）
- 修复 MCP 工具偶发永久缺失（loadMcpTools 空结果缓存）与配置迁移不落盘
- 修复 v0.6.6 遗留 lint 错误（2 处）

### Changed（变更）

- **EM 与 Claude Code 彻底解耦**：MCP 配置迁移至 `~/.easymint/mcp.json`（首次启动自动迁移旧配置）；Skill 目录迁移至 `~/.easymint/skills/`（全局与项目级）；CC 侧配置原样保留
- **开发记录按日期分文件**：`docs/开发记录.md`（快照+索引导航）+ `docs/开发记录/<日期>.md`（每天独立文件，按需读取）
- 内置模板排序：Mint-D 移至第二位；浏览/编辑表单统一（删除独立预览弹窗）
- 侧边栏底部按钮顺序（任务/运行/Issue）与 hover 效果
- 运行面板按钮区布局：停止为主按钮，重启/日志/打开 URL 图标化（窄面板不再挤压）

## v0.6.6 (2026-08-08) — 设备互联 & 跨设备项目迁移

### Added（新增）

- **设备互联**（工具箱→设备互联，实验性）：局域网设备发现（手动扫描）+ 蓝牙式配对（双向确认 + ECDH 密钥交换）+ WebSocket 连接 + 心跳保活
- **传输加密**：ECDH（P-256）派生共享密钥 + AES-256-GCM——配对密钥永不传输，迁移数据加密防嗅探
- **跨设备项目迁移**（纯手动）：设备面板「发送」→ 选项目（已打开列表/输入）→ 统一扫描（排除构建产物）→ zip 打包 → 两阶段握手 → 分块传输 → 接收端校验/解压/会话恢复
- **迁移完整性握手**：zip 整体 sha256 校验 + 解压文件数必须匹配 + 会话硬性保证（落盘验证 cwd）——两端双重确认，失败保留缓存包供排查
- **迁移完成卡片**：接收端弹出模板通知（说明已迁移到另一台电脑/会话文件完整 + 注意事项），复制发送给 Mint 对齐上下文
- **迁移步骤条**：两端完整步骤可视化（扫描/打包/等待确认/传输/校验/解压/会话恢复）
- **会话迁移**：最新主会话 jsonl 随 zip 迁移，首行 cwd 改写为接收端路径，会话列表自动可见可 resume
- **工具箱**：HTML 原型编辑器 + 设备互联，均标注「实验」标签
- **Windows 防火墙放行提示**（设备互联端口，一次性）

### Fixed（修复）

- 修复 Windows 控制台 GBK 乱码（detector 的 execSync 改 spawnSync 不走 cmd shell；dev 脚本绕过 .cmd 包装）
- 修复 Windows GPU/HTTP 磁盘缓存创建失败刷屏（禁用磁盘缓存走内存）
- 修复 Windows 虚拟网卡地址抢占（WSL/Hyper-V 172.x——取到虚拟网卡连不上，同网段优先匹配）
- 修复单方面解除配对抖动循环、广播冲突刷屏、重连广播泄漏（可被发现关闭仍互相可见）
- 修复配对记录地址缓存不对称（发起方重启后无法自动重连）、扫描器去重缓存（解除后自动扫描扫不出）
- 修复会话迁移多处链路：会话未进传输列表、cwd 反斜杠转义误判（文件在却报失败）、打包用项目名解析路径
- 修复迁移失败提示无具体信息（failures 数组被当文件数显示）
- 修复发送完成后卡「传输中」无法关闭、迁移:accept handler 被误删（接收报 No handler）
- 修复 archiver ESM 跨平台 bundle 不稳定、unzipper @aws-sdk 可选依赖打包失败
- 修复会话恢复链路（手动入口漏传会话、会话未进 zip 传输）

### Changed（变更）

- 设备互联完全手动：无自动扫描/自动重连/启动连接——按需使用（广播仅可被发现 60s、扫描手动 3s、连接手动触发）
- 迁移改纯手动：移除 Mint 迁移 skill 与 MCP 迁移工具，保留设备管理工具
- 迁移排除规则：Electron + Flutter 构建产物（node_modules/dist/build/.dart_tool/ephemeral/local.properties/*.iml 等），.easymint 项目状态保留
- 接收端选父文件夹自动创建项目文件夹；默认填软件默认项目目录
- 发送端显示已打开过的项目列表（免手动找路径）
- 呼吸灯渲染重构：离屏不透明合成彻底消除圆角/直线交接痕迹（与 dpr 无关）、实心环叠加亮度一致、光晕柔化

## v0.6.5 (2026-08-07) — 状态指示光效全面升级 & 聊天滚动体验 & 会话隔离

### Added（新增）

- **状态指示光效可配置化**（设置→界面）：3 预设（环绕流光/顶部滑动/呼吸灯）+ 关闭；单色/多色模式，多色支持自定义颜色组合分组（最多 5 组）、拖拽排序；亮色/暗色两套独立配置
- **光效三预设全面 canvas 化**：圆角路径精确贴合、彗尾半椭圆收尖、多色沿路径流转；呼吸灯改为由实向虚的发散柔光（10 层渐变），颜色跟随光带流动
- **呼吸灯渲染重构（离屏不透明合成）**：圆角/直线交接处痕迹彻底消除（与屏幕缩放 dpr 无关）；圆角与直线亮度一致；光晕柔化——内圈半透明有柔感、边缘虚化、视觉厚度更薄
- **光效参数固定模式**：粗细/速度/拖尾为组件常量（orbit 2/4s/4档、slide 2/4s/3档、breathe 3s 纯光晕），仅颜色可调——设置面板精简
- **Mint 状态文本样式可配置**：单色/流光，独立配色 + 亮暗两套
- **聊天回底/新消息气泡**：滚离底部常驻显示（流式中圆圈箭头、输出结束「新消息」胶囊），点击回底或手动滚回消失；悬浮在输入卡片上方居中
- **聊天滚动体验**：打开会话精确贴底、发送消息即时显示、流式输出不锁定（可自由翻阅历史）

### Fixed（修复）

- 修复 Windows 后台/前台命令输出乱码（UTF-8/GBK 自动判定，删除无效 LANG/LC_ALL）
- 修复会话隔离：状态信号/委派计数/上下文使用率按会话隔离，切换会话不串扰（架构级修复）
- 修复聊天滚动贴底状态机：打开会话/发送消息/流式跟随的连环问题（输入时间窗判定用户意图，程序性滚动永不误判）
- 修复颜色选择器触发区域过大（点击文字/空白误开取色器——label 包裹改 div）
- 修复 AgentBar/ShellBar zustand 无限重渲染、thinking 流式帧信号闪烁、ChatPanel TDZ 崩溃

### Changed（变更）

- 清理 agent/bg-shell 成功路径日志（启动/发消息不再刷屏，保留异常与低频事件日志）
- 状态栏符号动画与状态文本同现同消（输入卡片光效接管活跃指示）

## v0.6.4 (2026-08-06) — diff 语法高亮 & 工具结果展示 & Windows 后台命令修复

### Added（新增）

- **聊天区 diff 语法高亮**：edit 工具变更内容按语言级着色（关键词/字符串/注释/数字，17 种语言，零新依赖复用 monaco tokenizer）；红绿只做背景，文字保持语法色
- **cc 风格 diff 渲染**：行号列右对齐、`+ code`/`- code` 格式、批量 tokenize 消除首屏闪烁
- **edit 工具返回注入 diff**：执行后把变更内容追加进返回文本，Mint 可看到改了哪些行（主会话 + 子 Agent 会话）
- **工具结果进聊天区**：toolResult 消息按 toolCallId 关联到工具块显示，关闭"显示工具调用"时结果仍可见
- **Agent 过程弹层显示开关**：思考过程/工具调用默认隐藏（多选框样式），展开后带标签分层显示；子 Agent 的 edit 结果也显示红绿 diff

### Changed（变更）

- 工具卡片精简：edit/write/read 只显示路径（不再打印 edits/content 大段），标题右侧 `+N · -N` 变更统计，工具名统一 `edit`
- 失败工具调用红边框（打开/关闭开关显示一致）

### Fixed（修复）

- 修复 Windows 后台命令输出丢失（detached 导致 stdout/stderr 管道收不到数据）
- 修复 Windows 后台命令不支持 Git Bash 语法（`cd /c/`、`tail` 管道）——改用 Git Bash 执行，无 Git Bash 时明确报错
- 修复 Agent 过程弹层 React key 重复警告（keyId 与消息序号同步）

## v0.6.3 (2026-08-06) — 聊天字体调节 & 会话功能条 & UI 重构

### Added（新增）

- **聊天字体分级调节**（设置→界面）：6 级字体大小整体控制聊天界面（会话列表、消息气泡、思考与工具调用按层级同步缩放），Windows 上可调大提升可读性
- **"界面"设置分组**：独立的界面设置页
- **会话功能条**：会话列表顶部归档（时钟图标）与新建（气泡加号）平级并排，各自展开列表
- **Mint 掌控 Agent 生命周期**：查看子 Agent 执行过程（阶梯式读取）、精确停止单个 Agent、实时查看运行状态

### Changed（变更）

- 会话列表归档后自动刷新显示（轮转归档的会话出现在归档列表）
- 新建项目/会话/归档按钮 hover 统一中性色
- 项目标题加号、新建会话按钮改用标准 SVG 图标

### Fixed（修复）

- 修复用户主动打断 Agent/命令时 Mint 误判为意外失败并自动重启（明确显示"已由用户中断"）
- 修复非 DeepSeek 供应商余额查询报 404
- 修复消息气泡复制按钮在空白区域误触发

### 其他

- UI 组件合理化重构：设置对话框拆分独立 Tab、新建项目向导拆分步骤组件、聊天面板拆分气泡操作组件（纯搬移，行为不变）

## v0.6.2 (2026-08-05) — 委派进度卡片 & 便签交互优化

### Added（新增）

- **委派进度卡片计时**：标题栏右侧实时显示已耗时（超过 60 秒自动换算为分秒），委派完成时显示总耗时
- **委派任务列表完整显示**：并发受限时排队的任务也立即显示（等待中状态），不再等启动才出现
- **任务行点击展开详情**：默认显示任务简述，点击展开完整指令内容（自动换行）

### Changed（变更）

- 委派卡片附着点固定：Mint 发起委派后卡片稳定挂在触发消息下方，不再随消息流漂移
- 单任务被打断时 Mint 主动回应（确认中止并给下一步选项），批量中停止单个仍不打断
- 便签拖出窗口边缘可隐藏（露出 20px 可拖回），碰边收起更精准（需朝边缘外拖动）
- 便签宽度上限自适应窗口大小

### Fixed（修复）

- 修复委派进度卡片在部分任务完成/中断后消失的问题
- 修复便签拖动左/右边框时另一边跟随移动的问题
- 修复便签拖动后无法释放（松手后仍跟随鼠标）的问题
- 修复任务面板无滚动条、任务列表撑破面板的问题
- 修复委派进度面板标题与正文字号不一致的问题

## v0.6.1 (2026-08-05) — Agent 模板 & 自定义供应商

### Added（新增）

- **Agent 模板**：设置→Agent 页可新建/编辑自定义模板（名称、职责提示词、供应商、模型、思考级别），task 委派时可选用；Mint 也能一句话创建模板
- **自定义供应商**：支持自定义 Base URL、API 协议与模型列表的供应商，模型/兜底/子 Agent 默认模型可分别配置
- **全局思考等级**：设置→模型组可配置新聊天会话的默认思考等级
- **思考等级扩展为 7 档**：关闭 / 极简 / 低 / 中 / 高 / 超高 / 最大

### Changed（变更）

- 内置模板（Builder / Mint-D / Evaluator）支持编辑且持久生效；Mint 模板保持官方提示词只读
- task 委派不指定模板时使用标准子 Agent（适用于查资料、读代码等通用任务），写代码/验收时指定 builder / evaluator
- 供应商列表与模型下拉显示官方名称与图标（OpenCode 等品牌更新）

### Fixed（修复）

- 修复应用启动瞬间，新聊天会话可能未应用全局思考等级的问题

## v0.6.0 (2026-08-03) — 后台 Shell 可视化 & Agent 过程查看

### Added（新增）

- **后台命令**：bash 工具支持后台执行（不阻塞对话），完成自动通知；ShellBar 点击命令可实时查看输出（历史 + 实时流），完整日志落盘项目级 `.easymint/shell-logs/`（保留 7 天），支持随时停止
- **Agent 过程查看**：任务面板点击任务可查看子 Agent 完整执行过程（思考/文本/工具调用），历史 + 实时流追加
- **任务面板实时执行视图**：委派进度事件驱动任务行状态，任务计数移到标题行
- **task 关联 task.json**：委派可绑定任务 id，完成/中止自动回写状态（done/failed），无需手动标记

### Changed（变更）

- 系统消息改为结构化系统卡片（不再伪装成 AI 文本消息），完成/中止通知即时显示
- 单任务提前完成/被停止时立即通知（不等全部委派收尾）

### Fixed（修复）

- 修复委派结果注入失败（临时会话 ID 与真实 ID 不匹配）、恢复会话后工具缺失的问题
- 修复多条并发通知被吞、同批通知重复显示、通知插错回合位置的问题
- 修复打断按钮失效（front 传 chatId 而主进程只匹配 sessionId）、task 执行期间发消息无响应的问题
- 修复流式消息重复（turn 锚定消息全量替换）、思考过程逐词重复的问题
- 修复状态栏残留"调用中"（工具执行完未清）、等待泡偏离的问题
- 修复系统消息导致会话标题为"无消息"的问题

## v0.5.2 (2026-08-02) — Windows 窗口自绘 & UI 精致化

### 🪟 Windows 体验

- **自绘窗口按钮**：win32 隐藏系统标题栏（保留 Snap 布局/缩放/阴影），右上角自绘最小化/最大化/关闭按钮——主题跟随（亮暗自动适配）、最大化状态图标联动、关闭按钮 hover 红色（Windows 惯例）
- **移除默认菜单栏**：Windows/Linux 不再显示 Electron 默认 File/Edit/View/Window 菜单
- **字体渲染适配**：中文字体回退栈（PingFang SC / 微软雅黑）+ 小字号 12→13px（雅黑可读性）
- **布局对齐**：顶部拖拽区与 macOS 一致、TabBar 空态可拖拽、项目名称固定宽度超长 hover 滚动

### 🎨 UI 精致化

- **右键菜单**：宽度完全内容自适应、hover 高亮贴合圆角
- **删除项目确认**：EM 风格弹窗（提示移动到回收站/废纸篓，平台自适应）
- **浏览文件夹打开项目**：与列表点击一致，提示当前/新窗口
- **聊天区拖放上传**：拖入图片/文档不再触发系统打开文件
- **图标升级**：下拉菜单/空会话图标换 Lucide 素材（folder-plus/folder-open/pencil/copy/message-square）
- **Onboarding 风格对齐**：特性卡片 card 层、Logo 卡片容器、步骤指示器语义层

### 🔧 修复

- 关闭按钮 hover 红背景被 surface-hover 覆盖、空 tab 时 tab 栏不可拖拽、WindowControls 双挂载、项目名称过长顶走新建按钮

---
## v0.5.1 (2026-08-01) — 内容便签

### 📌 内容便签（v1-v5）

- **悬浮便签卡片**：AI 总结的大段内容可钉成悬浮卡片固定在聊天区，随时查看（会话级持久化，切换会话/重启不丢失）
- **双钉住入口**：消息气泡操作条一键钉整条；右键菜单（复制/全选/钉住）——选中文字钉住自动还原 Markdown（列表/标题/代码/加粗）
- **可调大小**：四周边缘/拐角自由拖拽调整尺寸，位置与大小持久化
- **书签式贴纸**：拖到边缘自动吸附折叠成彩色贴纸（左右边缘、8 色调色板随机分配不重复）；同边层叠可见（hover 抽出横条显示标题）；点击展开回卡片
- **重复检查**：同内容重复钉住自动拒绝并提示
- **吸附动画**：折叠/展开平滑过渡（贴纸滑入淡入、卡片缩放展开）
- **气泡操作条**：复制/钉住一体化工具条，hover 消息显示、离开 1s 缓冲
- **交互细节**：拖到边缘自动吸附、贴纸贴紧边缘单侧圆角、沿边拖动单独吸附、手动叠放自动错开

### 🎨 UI 微调

- 用户头像改为与 Mint 同风格（同底色仅字母区分），头像放大 20%
- 用户消息上方增加 USER 名称标签

---
## v0.5.0 (2026-07-31) — v3 UI 全面改版 & 上下文压缩增强

### 🎨 v3 UI 改版

- **布局**：三栏合并为双栏——250px 固定侧边栏 + 主区，纯色差分层（无分割线）
- **Layer 色彩体系**：L0-L4 层级色（canvas/sidebar/content/card/elevated），阴影代替边框区分
- **侧边栏**：项目名+菜单（新建/打开/重命名项目/新建窗口）、会话|文件 tabs、底部抽屉（任务/Issue/运行，点击外部关闭）、主题三模式切换
- **TabBar**：固定宽度、常显关闭按钮、flex 压缩（至 60px 下限）、marquee 滚动（标题超出时）
- **消息区**：M/U 头像+来源标签、悬浮复制按钮、气泡对齐原型（去边框、阴影分层）
- **会话列表**：今天/之前/更早分组（7 天边界）、时间移到标题右侧、项目会话圆点/设计会话菱形点
- **主题切换**：View Transitions 扩散动画（亮→暗左下角圆形揭开，暗→亮右上角）
- **编辑器**：暖乳白色系（去冷灰/绿）、Monaco 语法高亮 CSS 变量化、markdown 标题标准 token 修正
- **文件树**：folder/folder-open/file 图标（Lucide），展开状态视觉切换

### 🤖 上下文压缩增强

- **主动压缩**：上下文使用率达到阈值（默认 75%，设置可调）提前触发 compact——不等 Pi 自动压缩（近 100% 时性能已下降）
- **压缩状态机修复**：type 值不匹配（manual/compact）、compacting 永不清除（会锁死输入框）
- **轮转链路修复**：前端事件断链（rotate-create 从未广播）、Pi 原生摘要未接入 summaryBuffer（轮转从未真正执行）
- **轮转进度提示**：归档+新会话期间显示"正在整理并开启新会话..."

### ⚡ 性能

- **消息列表虚拟化**（@tanstack/react-virtual）：长对话 DOM 从数千节点降到 ~30，动态测量
- **busy 穿透优化**：回合切换时全量重渲染从 N 条降到 1 条
- **虚拟化 HMR 防御**：容器 state 驱动 + measure 兜底，开发模式热更新不丢状态

### 🔧 修复

- **会话置顶无效**：togglePin 返回 void 导致前端状态未更新
- **用户气泡布局**：flex 压缩导致逐字换行/位置乱飘（中文 min-content 陷阱）
- **打开会话不在底部**：虚拟化测量异步 + smooth 动画中断 autoScroll 误判
- **编辑器绿色调**：monaco 变量亮色全绿 + 主题切换不更新
- **md 标题红色**：Monaco 内置 markdown 标题 token 是 keyword（已修正为标准 markup.heading）
- **flushSync 报错**：虚拟化 measureElement 在 commit 阶段触发（useFlushSync: false）

### 🧹 工程

- **打包签名修复**：brand-tokens/linear.app 被 codesign 误判为 bundle（mac.signIgnore）——macOS 打包不再失败
- **Mint 项目进度链路清理**：删除已废弃的"项目进度鱼骨图"更新链路（set_project_stage 全链路）——Mint 不再更新鱼骨图面板

### 📄 文档

- **CHANGELOG/README/开发进度**：v0.5.0 全量记录、README 鱼骨图描述更新、开发进度新增第 9 章

## v0.4.1 (2026-07-30) — UI 细节优化 & Mint-D 增强

### 🎨 UI 细节优化

- **动画**：Tab 切换淡入淡出、按钮 transition 收窄（仅 colors/opacity/transform）、聊天平滑滚动、session 列表入场动画
- **色彩**：语义色补充 soft 层（danger/success/warning-soft）、暗色模式 warning/info 恢复语义色、文件树背景改为 bg-surface-alt
- **图标**：右侧栏图标全部换为语义化 SVG（看板/感叹号/播放圆圈）、README 图标路径修正
- **性能**：ChatPanel 消息 memo、zustand selector 细粒度化（避免 runningSessions 变化触发整页重渲染）、lastToolUses 用 useMemo 缓存

### 🤖 Mint-D 增强

- **品牌库**：内置 74 个品牌的 DESIGN.md（Airbnb、Stripe、Apple 等），创建设计会话时自动复制到 `.easymint/brand-tokens/`
- **品牌库被动参考**：Mint-D 知道品牌库存在但不主动读取，用户需要时列出候选品牌

### 🔧 修复

- **状态栏**：工具操作详情恢复（"检查代码: ChatPanel.tsx"、"执行: npm build"），不再被文本消息清空
- **MemoChatMessage**：hooks 顺序修复（React 严格模式报错）
- **displayToolLabel**：适配 Pi 小写工具名（read → 读取文件）
- **运行面板**：刷新按钮点击旋转动画
- **引导页**：图标路径修正

### ⚙️ 工程

- **供应商配置对齐 Pi 内置**：供应商列表直接读取 Pi 内置 Provider（静态 JSON 数据），与设置页供应商表单一致

## v0.4.0 (2026-07-29) — Pi SDK 迁移

### 🏗️ AI 引擎迁移

Claude SDK (`@anthropic-ai/claude-agent-sdk`) → Pi SDK (`@earendil-works/pi-coding-agent` v0.82.1)：

- 底层会话模型：`query()` + message channel → `createAgentSession()` + 事件流订阅 + `prompt()` 长生命周期会话
- 会话持久化：每会话独立 JSONL 落盘，支持中断恢复
- 流式输出：`message_update` 累计全文 → 按 turn 边界保留旧内容（多轮工具调用不再互相覆盖）
- 事件桥接：Pi `AgentSessionEvent` → 前端统一事件格式（text/toolCall/thinking 同处理）

### 🛡️ 权限系统修复

- 修复工具权限匹配失效（工具名变形导致 `canUseTool` 全不匹配）——权限控制恢复正常
- 内置 UI 工具加入安全白名单，不再频繁弹确认
- 工具执行时状态栏正确显示工具名

### 🎭 角色模板修复

- 选择模板时用模板提示词**替代**默认 Mint 提示词（不再叠加两套身份）——设计师/Builder 角色独立生效

### 📁 路径修正

- 修复项目级 `.easymint/` 被批量错写为全局路径的问题（run.json、issues.json 等）

### 🔒 多 Tab 事件隔离

- 修复多个聊天标签页事件串扰（runId 判断失效导致事件过滤失灵）

### 🚦 状态栏优化

- 文本到达时正确清除"正在请求..."与旧工具标签

### 📝 文档更新

- 技术架构、配置路径、SDK 使用对照、README、CLAUDE.md 全量更新至 Pi SDK

### 🤖 CI/CD

- 新增 GitHub Actions 自动发布：推送 tag（v*）自动构建 macOS + Windows 双平台 Release

---

## v0.3.0 (2026-06-25)

### Added（新增）

- **Skill 分级注入**：内置 Skill 与全局 Skill 两层分级管理
- **Hook 校验系统**：工具调用前状态一致性校验，防止异常状态执行

### Changed（变更）

- **Compact 体验优化**：压缩状态显示 + 输入框蒙版（压缩期间不可输入）
- **会话历史直读 JSONL**：不再受 compact 压缩链（parentUuid）限制，历史完整可查

### Fixed（修复）

- 新建项目跳转、会话删除后复活、MCP 配置热刷新等多个 Bug
