# 用最便宜的模型，做出专业级的桌面编程 Agent

> 我的 AI 编程故事——从 14 个文件的 shell 模板，到 3.6 万行 TypeScript 的桌面产品。全程 99% 用 DeepSeek-v4，7 月起甚至是最便宜的 flash 档。

## 起点：一个 shell 脚本模板

2026 年 5 月，我手里有一个叫 `ai-coding-automation-template` 的模板。它一共 14 个文件、8 个代码文件：

- `init.sh`——初始化项目
- `run-automation.sh`——跑一轮自动化开发
- `evaluate.sh` + `EVALUATOR.md`——让 AI 评估结果
- `WORKER.md`、`CLAUDE.md`——给 AI 看的规则文本
- `task.json`——任务清单，每个任务带着 `"passes": false`
- `progress.txt`——一个纯文本的「会话交接日志」，让下一个 AI 会话接着干

它是个能跑通的东西，但也仅此而已：填占位符、跑脚本、读文本交接。所有状态活在 shell 参数和 txt 里，所有交互发生在终端里——一个不懂技术的人拿到它，无从下手。

## 从脚本到产品

我真正的目标不是做一个给自己用的自动化脚本，而是：**让完全不懂技术的人，也能靠 AI 做出软件**。这样的人不会开终端、不会读 `progress.txt`、不会改 `task.json`——他们需要一个图形界面：新建项目、说需求、看 AI 干活、验收结果。

EasyMint 就是这么长出来的。它继承了模板的全部 DNA，但换了一副血肉：

| 模板时代 | EasyMint 今天 |
|---|---|
| `run-automation.sh` | Mint——长生命周期 AI 会话，事件流驱动 |
| `evaluate.sh` + `EVALUATOR.md` | Evaluator 委派 Agent，按 task.json 分层验收 |
| `WORKER.md` | Builder 委派 worker，独立工具集 |
| `progress.txt` | 文档体系：进度快照 + 索引 + 按日期明细 |
| `init.sh` | 图形化的创建引导，对话式采集需求 |
| task.json 骨架 | 正式任务状态机（直到今天才清掉模板遗留的 `passes` 字段） |

如今的主仓库里，是 174 个源文件、约 3.6 万行 TypeScript、49 个主进程服务：权限系统（标准/完全访问两模式 + 判不了域的命令进 OS 级沙盒）、三套清单体系（用户待办/执行追踪/正式任务）、应用内图片查看器、跨平台 CI 冒烟——从 7 月 23 日建仓起 806 次提交，发到 v0.20.0。

## 条件：全程最便宜的模型

到这里，需要交代一个反常识的前提：**这 806 次提交里，99% 不是顶级模型写的**。我没有用 Claude 的旗舰或 ChatGPT 的顶配——绝大部分工作由 DeepSeek-v4 完成，7 月开始甚至降档到更便宜的 flash 模型。

是预算不够吗？不是。是我在验证一个猜想：**AI 编程的上限到底由什么决定——模型的智力，还是协作的方法？**

## 凭什么做成：把「思考方式」写进规则

做 EasyMint 的过程里，我和我的 AI 协作者撞过大量失败。有意思的是，失败很少因为模型"笨"，更多因为它**猜**：猜我指哪个组件、猜我说的"大气"是什么意思、猜坏了就顺手改。每撞一次，我们就沉淀一条规则，把高级模型的审慎变成强制步骤：

- **记忆弱？** 不依赖模型记得，靠磁盘记得——文档分层、唯一真相源、方案定稿即落盘。上下文压缩丢得了对话，丢不了文档。
- **爱猜？** 需求理解立规则：位置-现象-目标三要素先对齐；方向词要复述确认；同一表象多个实现（「侧边弹出」是抽屉还是侧滑面板？）要列候选；连续被纠正两次就停止猜测、回到原话。这套规则来自 **66 个真实失败案例**的分类沉淀。
- **查问题靠猜？** 规矩是优先加日志、不要猜；查不明原因就按理想化预期重写逻辑——状态广播完备、唯一真相源、消除竞态、失败路径兜底。
- **质量不稳？** 质量关从模型手里移到机制手里：改完必过 lint 三段；系统提示词是模板字符串、改完必须 build 验证；关键字符串契约有自动化测试锚定；提交信息与 CHANGELOG 只写客观变更、不写过程感慨。
- **交付靠嘴说？** 完成 = 验证生效；用户实测即真相——用户说有问题就是有问题，不归因用户环境。

这些规矩的合集，就是项目的 CLAUDE.md 和几百行系统提示词。它们不是装饰——**每一次"模型不行"的失败，最后都追到了某条没写下来的规则上；每一条规则写下来之后，同样的失败就不再出现。**

## 这个故事的真正内核

回头看，「便宜模型做出复杂软件」的反常识等式，拆开其实是：**模型负责生成，方法论负责思考**。模型的智能是放大器，而放大什么、放大多少，由协作纪律决定——而纪律是可以跨模型复用的：今天这个模型便宜，明天换一个贵的，规则照跑；反过来，给最强模型配最烂的流程，它也会在同一个坑里反复跌。

这也是为什么这个故事和 EasyMint 是同一个故事：产品要卖给小白的是"AI 编程可以人人可用"，我亲身验证的版本是"最便宜的 AI 也够用"——一个是用 AI 写软件的故事，一个是写"用 AI 写软件的工具"的故事，内核同一句：**方法比工具重要。**

最便宜的模型 + 最严苛的协作纪律 = 专业级的产出。这个等式我验证了三个月、806 次提交。它现在跑在一个桌面产品里，等着下一个验证它的人。
